---
"solana_kit_fixed_points": major
---

# Add Fixed-Points arithmetic package

Add `solana_kit_fixed_points` with `I80f48` and `U64x64` fixed-point number types, arithmetic operators, and conversion helpers.
