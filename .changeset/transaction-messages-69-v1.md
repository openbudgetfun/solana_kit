---
"solana_kit_transaction_messages": patch
---

# Add full transaction message v1 support

Add full transaction message v1 support: v1 config values and masks, compile/encode/decode paths, instruction headers and payloads, compute-unit config helpers, version-aware message limits, value equality for v1 compiled helper types, and regression coverage for static account preservation.
