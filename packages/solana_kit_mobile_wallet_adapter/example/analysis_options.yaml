include: package:solana_kit_lints/flutter_analysis_options.yaml

linter:
  rules:
    # Example widgets and controllers are not published API, so keep the shared
    # Flutter safety rules but relax package API documentation requirements.
    public_member_api_docs: false
    diagnostic_describe_all_properties: false
