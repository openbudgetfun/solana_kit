name: solana_kit_codecs_strings
description: String codecs for the Solana Kit Dart SDK.
version: 0.1.0
repository: https://github.com/openbudgetfun/solana_kit

environment:
  sdk: ^3.10.1

resolution: workspace

dependencies:
  solana_kit_codecs_core:
    workspace: true
  solana_kit_codecs_numbers:
    workspace: true
  solana_kit_errors:
    workspace: true

dev_dependencies:
  solana_kit_lints:
    workspace: true
  test: ^1.25.0
