name: solana_kit_rpc_parsed_types
description: Parsed account data types for the Solana Kit Dart SDK.
version: 0.0.2

environment:
  sdk: ^3.10.1

resolution: workspace

dependencies:
  solana_kit_addresses:
  solana_kit_errors:
  solana_kit_rpc_types:

dev_dependencies:
  solana_kit_lints:
  test: ^1.25.8
