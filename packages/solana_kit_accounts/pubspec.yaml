name: solana_kit_accounts
description: Account fetching and decoding for the Solana Kit Dart SDK.
version: 0.1.0
repository: https://github.com/openbudgetfun/solana_kit

environment:
  sdk: ^3.10.1

resolution: workspace

dependencies:
  meta: ^1.16.0
  solana_kit_addresses: ^0.1.0
  solana_kit_codecs_core: ^0.1.0
  solana_kit_codecs_strings: ^0.1.0
  solana_kit_errors: ^0.1.0
  solana_kit_rpc_spec: ^0.1.0
  solana_kit_rpc_types: ^0.1.0

dev_dependencies:
  solana_kit_lints: ^0.1.0
  test: ^1.25.0
