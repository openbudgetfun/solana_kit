name: solana_kit_instruction_plans
description: Instruction planning for the Solana Kit Dart SDK.
version: 0.0.1

environment:
  sdk: ^3.10.1

resolution: workspace

dependencies:
  solana_kit_errors:
  solana_kit_instructions:

dev_dependencies:
  solana_kit_lints:
