{
  "schema_version": "0.6",
  "kind": "monochange.releaseRecord",
  "created_at": "2026-09-21T00:17:28Z",
  "command": "release",
  "version": null,
  "versions": {
    "codama-renderers-dart": "0.5.6",
    "main": "0.10.0",
    "solana_kit_address_lookup_table": "0.5.0",
    "solana_kit_associated_token_account": "0.8.0",
    "solana_kit_attestation_service": "0.2.0",
    "solana_kit_compute_budget": "0.9.0",
    "solana_kit_config": "0.3.0",
    "solana_kit_dapp_publisher_cli": "0.2.0",
    "solana_kit_helius": "0.7.0",
    "solana_kit_integration_tests": "0.2.0",
    "solana_kit_loader": "0.5.0",
    "solana_kit_memo": "0.5.0",
    "solana_kit_mobile_wallet_adapter": "0.6.0",
    "solana_kit_mobile_wallet_adapter_protocol": "0.6.0",
    "solana_kit_mpl_bubblegum": "0.6.0",
    "solana_kit_offchain_messages": "0.6.0",
    "solana_kit_spl_account_compression": "0.5.0",
    "solana_kit_stake": "0.6.0",
    "solana_kit_subscriptions": "0.3.0",
    "solana_kit_surfpool": "0.4.0",
    "solana_kit_system": "0.8.0",
    "solana_kit_sysvars": "0.6.0",
    "solana_kit_token": "0.9.0",
    "solana_kit_token_2022": "0.9.0",
    "wallet": "0.2.0"
  },
  "release_targets": [
    {
      "id": "codama-renderers-dart",
      "kind": "package",
      "version": "0.5.6",
      "version_format": "namespaced",
      "tag": true,
      "release": true,
      "tag_name": "codama-renderers-dart/v0.5.6",
      "members": [
        "codama-renderers-dart"
      ]
    },
    {
      "id": "main",
      "kind": "group",
      "version": "0.10.0",
      "version_format": "primary",
      "tag": true,
      "release": true,
      "tag_name": "v0.10.0",
      "members": [
        "solana_kit",
        "solana_kit_accounts",
        "solana_kit_address",
        "solana_kit_anchor",
        "solana_kit_address_constants",
        "solana_kit_addresses",
        "solana_kit_codecs",
        "solana_kit_codecs_core",
        "solana_kit_codecs_data_structures",
        "solana_kit_codecs_numbers",
        "solana_kit_codecs_strings",
        "solana_kit_fixed_points",
        "solana_kit_errors",
        "solana_kit_fast_stable_stringify",
        "solana_kit_jupiter",
        "solana_kit_instruction_plans",
        "solana_kit_instructions",
        "solana_kit_keys",
        "solana_kit_lints",
        "solana_kit_mpl_core",
        "solana_kit_mpl_token_metadata",
        "solana_kit_options",
        "solana_kit_pyth",
        "solana_kit_program_client_core",
        "solana_kit_programs",
        "solana_kit_rpc",
        "solana_kit_rpc_api",
        "solana_kit_rpc_parsed_types",
        "solana_kit_rpc_spec",
        "solana_kit_rpc_spec_types",
        "solana_kit_rpc_subscriptions",
        "solana_kit_rpc_subscriptions_api",
        "solana_kit_rpc_subscriptions_channel_websocket",
        "solana_kit_rpc_transformers",
        "solana_kit_rpc_transport_http",
        "solana_kit_rpc_types",
        "solana_kit_signers",
        "solana_kit_sns",
        "solana_kit_squads",
        "solana_kit_subscribable",
        "solana_kit_test_matchers",
        "solana_kit_transaction_confirmation",
        "solana_kit_transaction_introspection",
        "solana_kit_transaction_messages",
        "solana_kit_transactions"
      ]
    },
    {
      "id": "solana_kit_address_lookup_table",
      "kind": "package",
      "version": "0.5.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_address_lookup_table/v0.5.0",
      "members": [
        "solana_kit_address_lookup_table"
      ]
    },
    {
      "id": "solana_kit_associated_token_account",
      "kind": "package",
      "version": "0.8.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_associated_token_account/v0.8.0",
      "members": [
        "solana_kit_associated_token_account"
      ]
    },
    {
      "id": "solana_kit_attestation_service",
      "kind": "package",
      "version": "0.2.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_attestation_service/v0.2.0",
      "members": [
        "solana_kit_attestation_service"
      ]
    },
    {
      "id": "solana_kit_compute_budget",
      "kind": "package",
      "version": "0.9.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_compute_budget/v0.9.0",
      "members": [
        "solana_kit_compute_budget"
      ]
    },
    {
      "id": "solana_kit_config",
      "kind": "package",
      "version": "0.3.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_config/v0.3.0",
      "members": [
        "solana_kit_config"
      ]
    },
    {
      "id": "solana_kit_dapp_publisher_cli",
      "kind": "package",
      "version": "0.2.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_dapp_publisher_cli/v0.2.0",
      "members": [
        "solana_kit_dapp_publisher_cli"
      ]
    },
    {
      "id": "solana_kit_helius",
      "kind": "package",
      "version": "0.7.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_helius/v0.7.0",
      "members": [
        "solana_kit_helius"
      ]
    },
    {
      "id": "solana_kit_integration_tests",
      "kind": "package",
      "version": "0.2.0",
      "version_format": "namespaced",
      "tag": false,
      "release": false,
      "tag_name": "solana_kit_integration_tests/v0.2.0",
      "members": [
        "solana_kit_integration_tests"
      ]
    },
    {
      "id": "solana_kit_loader",
      "kind": "package",
      "version": "0.5.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_loader/v0.5.0",
      "members": [
        "solana_kit_loader"
      ]
    },
    {
      "id": "solana_kit_memo",
      "kind": "package",
      "version": "0.5.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_memo/v0.5.0",
      "members": [
        "solana_kit_memo"
      ]
    },
    {
      "id": "solana_kit_mobile_wallet_adapter",
      "kind": "package",
      "version": "0.6.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_mobile_wallet_adapter/v0.6.0",
      "members": [
        "solana_kit_mobile_wallet_adapter"
      ]
    },
    {
      "id": "solana_kit_mobile_wallet_adapter_protocol",
      "kind": "package",
      "version": "0.6.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_mobile_wallet_adapter_protocol/v0.6.0",
      "members": [
        "solana_kit_mobile_wallet_adapter_protocol"
      ]
    },
    {
      "id": "solana_kit_mpl_bubblegum",
      "kind": "package",
      "version": "0.6.0",
      "version_format": "namespaced",
      "tag": false,
      "release": false,
      "tag_name": "solana_kit_mpl_bubblegum/v0.6.0",
      "members": [
        "solana_kit_mpl_bubblegum"
      ]
    },
    {
      "id": "solana_kit_offchain_messages",
      "kind": "package",
      "version": "0.6.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_offchain_messages/v0.6.0",
      "members": [
        "solana_kit_offchain_messages"
      ]
    },
    {
      "id": "solana_kit_spl_account_compression",
      "kind": "package",
      "version": "0.5.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_spl_account_compression/v0.5.0",
      "members": [
        "solana_kit_spl_account_compression"
      ]
    },
    {
      "id": "solana_kit_stake",
      "kind": "package",
      "version": "0.6.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_stake/v0.6.0",
      "members": [
        "solana_kit_stake"
      ]
    },
    {
      "id": "solana_kit_subscriptions",
      "kind": "package",
      "version": "0.3.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_subscriptions/v0.3.0",
      "members": [
        "solana_kit_subscriptions"
      ]
    },
    {
      "id": "solana_kit_surfpool",
      "kind": "package",
      "version": "0.4.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_surfpool/v0.4.0",
      "members": [
        "solana_kit_surfpool"
      ]
    },
    {
      "id": "solana_kit_system",
      "kind": "package",
      "version": "0.8.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_system/v0.8.0",
      "members": [
        "solana_kit_system"
      ]
    },
    {
      "id": "solana_kit_sysvars",
      "kind": "package",
      "version": "0.6.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_sysvars/v0.6.0",
      "members": [
        "solana_kit_sysvars"
      ]
    },
    {
      "id": "solana_kit_token",
      "kind": "package",
      "version": "0.9.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_token/v0.9.0",
      "members": [
        "solana_kit_token"
      ]
    },
    {
      "id": "solana_kit_token_2022",
      "kind": "package",
      "version": "0.9.0",
      "version_format": "namespaced",
      "tag": true,
      "release": false,
      "tag_name": "solana_kit_token_2022/v0.9.0",
      "members": [
        "solana_kit_token_2022"
      ]
    },
    {
      "id": "wallet",
      "kind": "group",
      "version": "0.2.0",
      "version_format": "namespaced",
      "tag": true,
      "release": true,
      "tag_name": "wallet/v0.2.0",
      "members": [
        "solana_kit_wallet_adapter",
        "solana_kit_wallet_standard",
        "solana_kit_wallet_ui"
      ]
    }
  ],
  "released_packages": [
    "codama-renderers-dart",
    "solana_kit",
    "solana_kit_accounts",
    "solana_kit_address",
    "solana_kit_address_constants",
    "solana_kit_address_lookup_table",
    "solana_kit_addresses",
    "solana_kit_anchor",
    "solana_kit_associated_token_account",
    "solana_kit_attestation_service",
    "solana_kit_codecs",
    "solana_kit_codecs_core",
    "solana_kit_codecs_data_structures",
    "solana_kit_codecs_numbers",
    "solana_kit_codecs_strings",
    "solana_kit_compute_budget",
    "solana_kit_config",
    "solana_kit_dapp_publisher_cli",
    "solana_kit_errors",
    "solana_kit_fast_stable_stringify",
    "solana_kit_fixed_points",
    "solana_kit_helius",
    "solana_kit_instruction_plans",
    "solana_kit_instructions",
    "solana_kit_integration_tests",
    "solana_kit_jupiter",
    "solana_kit_keys",
    "solana_kit_lints",
    "solana_kit_loader",
    "solana_kit_memo",
    "solana_kit_mobile_wallet_adapter",
    "solana_kit_mobile_wallet_adapter_protocol",
    "solana_kit_mpl_bubblegum",
    "solana_kit_mpl_core",
    "solana_kit_mpl_token_metadata",
    "solana_kit_offchain_messages",
    "solana_kit_options",
    "solana_kit_program_client_core",
    "solana_kit_programs",
    "solana_kit_pyth",
    "solana_kit_rpc",
    "solana_kit_rpc_api",
    "solana_kit_rpc_parsed_types",
    "solana_kit_rpc_spec",
    "solana_kit_rpc_spec_types",
    "solana_kit_rpc_subscriptions",
    "solana_kit_rpc_subscriptions_api",
    "solana_kit_rpc_subscriptions_channel_websocket",
    "solana_kit_rpc_transformers",
    "solana_kit_rpc_transport_http",
    "solana_kit_rpc_types",
    "solana_kit_signers",
    "solana_kit_sns",
    "solana_kit_spl_account_compression",
    "solana_kit_squads",
    "solana_kit_stake",
    "solana_kit_subscribable",
    "solana_kit_subscriptions",
    "solana_kit_surfpool",
    "solana_kit_system",
    "solana_kit_sysvars",
    "solana_kit_test_matchers",
    "solana_kit_token",
    "solana_kit_token_2022",
    "solana_kit_transaction_confirmation",
    "solana_kit_transaction_introspection",
    "solana_kit_transaction_messages",
    "solana_kit_transactions",
    "solana_kit_wallet_adapter",
    "solana_kit_wallet_standard",
    "solana_kit_wallet_ui"
  ],
  "changed_files": [
    "CHANGELOG.md",
    "docs/site/pubspec.yaml",
    "examples/pubspec.yaml",
    "packages/codama-renderers-dart/CHANGELOG.md",
    "packages/codama-renderers-dart/package.json",
    "packages/codama-renderers-dart/pnpm-lock.yaml",
    "packages/codama-renderers-dart/test-generated/pubspec.yaml",
    "packages/solana_kit/CHANGELOG.md",
    "packages/solana_kit/pubspec.yaml",
    "packages/solana_kit_accounts/CHANGELOG.md",
    "packages/solana_kit_accounts/pubspec.yaml",
    "packages/solana_kit_address/CHANGELOG.md",
    "packages/solana_kit_address/pubspec.yaml",
    "packages/solana_kit_address_constants/CHANGELOG.md",
    "packages/solana_kit_address_constants/pubspec.yaml",
    "packages/solana_kit_address_lookup_table/CHANGELOG.md",
    "packages/solana_kit_address_lookup_table/pubspec.yaml",
    "packages/solana_kit_addresses/CHANGELOG.md",
    "packages/solana_kit_addresses/pubspec.yaml",
    "packages/solana_kit_anchor/CHANGELOG.md",
    "packages/solana_kit_anchor/pubspec.yaml",
    "packages/solana_kit_associated_token_account/CHANGELOG.md",
    "packages/solana_kit_associated_token_account/pubspec.yaml",
    "packages/solana_kit_attestation_service/CHANGELOG.md",
    "packages/solana_kit_attestation_service/pubspec.yaml",
    "packages/solana_kit_codecs/CHANGELOG.md",
    "packages/solana_kit_codecs/pubspec.yaml",
    "packages/solana_kit_codecs_core/CHANGELOG.md",
    "packages/solana_kit_codecs_core/pubspec.yaml",
    "packages/solana_kit_codecs_data_structures/CHANGELOG.md",
    "packages/solana_kit_codecs_data_structures/pubspec.yaml",
    "packages/solana_kit_codecs_numbers/CHANGELOG.md",
    "packages/solana_kit_codecs_numbers/pubspec.yaml",
    "packages/solana_kit_codecs_strings/CHANGELOG.md",
    "packages/solana_kit_codecs_strings/pubspec.yaml",
    "packages/solana_kit_compute_budget/CHANGELOG.md",
    "packages/solana_kit_compute_budget/pubspec.yaml",
    "packages/solana_kit_config/CHANGELOG.md",
    "packages/solana_kit_config/pubspec.yaml",
    "packages/solana_kit_dapp_publisher_cli/CHANGELOG.md",
    "packages/solana_kit_dapp_publisher_cli/pubspec.yaml",
    "packages/solana_kit_errors/CHANGELOG.md",
    "packages/solana_kit_errors/pubspec.yaml",
    "packages/solana_kit_fast_stable_stringify/CHANGELOG.md",
    "packages/solana_kit_fast_stable_stringify/pubspec.yaml",
    "packages/solana_kit_fixed_points/CHANGELOG.md",
    "packages/solana_kit_fixed_points/pubspec.yaml",
    "packages/solana_kit_helius/CHANGELOG.md",
    "packages/solana_kit_helius/pubspec.yaml",
    "packages/solana_kit_instruction_plans/CHANGELOG.md",
    "packages/solana_kit_instruction_plans/pubspec.yaml",
    "packages/solana_kit_instructions/CHANGELOG.md",
    "packages/solana_kit_instructions/pubspec.yaml",
    "packages/solana_kit_integration_tests/CHANGELOG.md",
    "packages/solana_kit_integration_tests/pubspec.yaml",
    "packages/solana_kit_jupiter/CHANGELOG.md",
    "packages/solana_kit_jupiter/pubspec.yaml",
    "packages/solana_kit_keys/CHANGELOG.md",
    "packages/solana_kit_keys/pubspec.yaml",
    "packages/solana_kit_lints/CHANGELOG.md",
    "packages/solana_kit_lints/pubspec.yaml",
    "packages/solana_kit_loader/CHANGELOG.md",
    "packages/solana_kit_loader/pubspec.yaml",
    "packages/solana_kit_memo/CHANGELOG.md",
    "packages/solana_kit_memo/pubspec.yaml",
    "packages/solana_kit_mobile_wallet_adapter/CHANGELOG.md",
    "packages/solana_kit_mobile_wallet_adapter/example/pubspec.yaml",
    "packages/solana_kit_mobile_wallet_adapter/pubspec.yaml",
    "packages/solana_kit_mobile_wallet_adapter_protocol/CHANGELOG.md",
    "packages/solana_kit_mobile_wallet_adapter_protocol/pubspec.yaml",
    "packages/solana_kit_mpl_bubblegum/CHANGELOG.md",
    "packages/solana_kit_mpl_bubblegum/pubspec.yaml",
    "packages/solana_kit_mpl_core/CHANGELOG.md",
    "packages/solana_kit_mpl_core/pubspec.yaml",
    "packages/solana_kit_mpl_token_metadata/CHANGELOG.md",
    "packages/solana_kit_mpl_token_metadata/pubspec.yaml",
    "packages/solana_kit_offchain_messages/CHANGELOG.md",
    "packages/solana_kit_offchain_messages/pubspec.yaml",
    "packages/solana_kit_options/CHANGELOG.md",
    "packages/solana_kit_options/pubspec.yaml",
    "packages/solana_kit_program_client_core/CHANGELOG.md",
    "packages/solana_kit_program_client_core/pubspec.yaml",
    "packages/solana_kit_programs/CHANGELOG.md",
    "packages/solana_kit_programs/pubspec.yaml",
    "packages/solana_kit_pyth/CHANGELOG.md",
    "packages/solana_kit_pyth/pubspec.yaml",
    "packages/solana_kit_rpc/CHANGELOG.md",
    "packages/solana_kit_rpc/pubspec.yaml",
    "packages/solana_kit_rpc_api/CHANGELOG.md",
    "packages/solana_kit_rpc_api/pubspec.yaml",
    "packages/solana_kit_rpc_parsed_types/CHANGELOG.md",
    "packages/solana_kit_rpc_parsed_types/pubspec.yaml",
    "packages/solana_kit_rpc_spec/CHANGELOG.md",
    "packages/solana_kit_rpc_spec/pubspec.yaml",
    "packages/solana_kit_rpc_spec_types/CHANGELOG.md",
    "packages/solana_kit_rpc_spec_types/pubspec.yaml",
    "packages/solana_kit_rpc_subscriptions/CHANGELOG.md",
    "packages/solana_kit_rpc_subscriptions/pubspec.yaml",
    "packages/solana_kit_rpc_subscriptions_api/CHANGELOG.md",
    "packages/solana_kit_rpc_subscriptions_api/pubspec.yaml",
    "packages/solana_kit_rpc_subscriptions_channel_websocket/CHANGELOG.md",
    "packages/solana_kit_rpc_subscriptions_channel_websocket/pubspec.yaml",
    "packages/solana_kit_rpc_transformers/CHANGELOG.md",
    "packages/solana_kit_rpc_transformers/pubspec.yaml",
    "packages/solana_kit_rpc_transport_http/CHANGELOG.md",
    "packages/solana_kit_rpc_transport_http/pubspec.yaml",
    "packages/solana_kit_rpc_types/CHANGELOG.md",
    "packages/solana_kit_rpc_types/pubspec.yaml",
    "packages/solana_kit_signers/CHANGELOG.md",
    "packages/solana_kit_signers/pubspec.yaml",
    "packages/solana_kit_sns/CHANGELOG.md",
    "packages/solana_kit_sns/pubspec.yaml",
    "packages/solana_kit_spl_account_compression/CHANGELOG.md",
    "packages/solana_kit_spl_account_compression/pubspec.yaml",
    "packages/solana_kit_squads/CHANGELOG.md",
    "packages/solana_kit_squads/pubspec.yaml",
    "packages/solana_kit_stake/CHANGELOG.md",
    "packages/solana_kit_stake/pubspec.yaml",
    "packages/solana_kit_subscribable/CHANGELOG.md",
    "packages/solana_kit_subscribable/pubspec.yaml",
    "packages/solana_kit_subscriptions/CHANGELOG.md",
    "packages/solana_kit_subscriptions/pubspec.yaml",
    "packages/solana_kit_surfpool/CHANGELOG.md",
    "packages/solana_kit_surfpool/pubspec.yaml",
    "packages/solana_kit_system/CHANGELOG.md",
    "packages/solana_kit_system/pubspec.yaml",
    "packages/solana_kit_sysvars/CHANGELOG.md",
    "packages/solana_kit_sysvars/pubspec.yaml",
    "packages/solana_kit_test_matchers/CHANGELOG.md",
    "packages/solana_kit_test_matchers/pubspec.yaml",
    "packages/solana_kit_token/CHANGELOG.md",
    "packages/solana_kit_token/pubspec.yaml",
    "packages/solana_kit_token_2022/CHANGELOG.md",
    "packages/solana_kit_token_2022/pubspec.yaml",
    "packages/solana_kit_transaction_confirmation/CHANGELOG.md",
    "packages/solana_kit_transaction_confirmation/pubspec.yaml",
    "packages/solana_kit_transaction_introspection/CHANGELOG.md",
    "packages/solana_kit_transaction_introspection/pubspec.yaml",
    "packages/solana_kit_transaction_messages/CHANGELOG.md",
    "packages/solana_kit_transaction_messages/pubspec.yaml",
    "packages/solana_kit_transactions/CHANGELOG.md",
    "packages/solana_kit_transactions/pubspec.yaml",
    "packages/solana_kit_wallet_adapter/CHANGELOG.md",
    "packages/solana_kit_wallet_adapter/pubspec.yaml",
    "packages/solana_kit_wallet_standard/CHANGELOG.md",
    "packages/solana_kit_wallet_standard/pubspec.yaml",
    "packages/solana_kit_wallet_ui/CHANGELOG.md",
    "packages/solana_kit_wallet_ui/example/pubspec.yaml",
    "packages/solana_kit_wallet_ui/pubspec.yaml",
    "pubspec.yaml",
    "versions.json"
  ],
  "package_publications": [
    {
      "package": "codama-renderers-dart",
      "ecosystem": "npm",
      "registry": "npm",
      "version": "0.5.6",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_accounts",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_address",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_address_constants",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_address_lookup_table",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.5.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_addresses",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_anchor",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_associated_token_account",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.8.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_attestation_service",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.2.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "preferred",
        "repository": "openbudgetfun/solana_kit",
        "workflow": "publish.yml",
        "environment": "publisher"
      }
    },
    {
      "package": "solana_kit_codecs",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_codecs_core",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_codecs_data_structures",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_codecs_numbers",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_codecs_strings",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_compute_budget",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.9.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_config",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.3.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_dapp_publisher_cli",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.2.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_errors",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_fast_stable_stringify",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_fixed_points",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_helius",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.7.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_instruction_plans",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_instructions",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_jupiter",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_keys",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_lints",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_loader",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.5.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_memo",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.5.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_mobile_wallet_adapter",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.6.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_mobile_wallet_adapter_protocol",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.6.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_mpl_bubblegum",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.6.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_mpl_core",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_mpl_token_metadata",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_offchain_messages",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.6.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_options",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_program_client_core",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_programs",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_pyth",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_rpc",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_rpc_api",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_rpc_parsed_types",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_rpc_spec",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_rpc_spec_types",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_rpc_subscriptions",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_rpc_subscriptions_api",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_rpc_subscriptions_channel_websocket",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_rpc_transformers",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_rpc_transport_http",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_rpc_types",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_signers",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_sns",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_spl_account_compression",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.5.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_squads",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_stake",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.6.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_subscribable",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_subscriptions",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.3.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_surfpool",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.4.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_system",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.8.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_sysvars",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.6.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_token",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.9.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_token_2022",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.9.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_transaction_confirmation",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_transaction_introspection",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_transaction_messages",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_transactions",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.10.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_wallet_adapter",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.2.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_wallet_standard",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.2.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    },
    {
      "package": "solana_kit_wallet_ui",
      "ecosystem": "dart",
      "registry": "pub_dev",
      "version": "0.2.0",
      "mode": "builtin",
      "trusted_publishing": {
        "enabled": true,
        "mode": "required",
        "repository": null,
        "workflow": null,
        "environment": null
      }
    }
  ],
  "updated_changelogs": [
    "CHANGELOG.md",
    "packages/codama-renderers-dart/CHANGELOG.md",
    "packages/solana_kit/CHANGELOG.md",
    "packages/solana_kit_accounts/CHANGELOG.md",
    "packages/solana_kit_address/CHANGELOG.md",
    "packages/solana_kit_address_constants/CHANGELOG.md",
    "packages/solana_kit_address_lookup_table/CHANGELOG.md",
    "packages/solana_kit_addresses/CHANGELOG.md",
    "packages/solana_kit_anchor/CHANGELOG.md",
    "packages/solana_kit_associated_token_account/CHANGELOG.md",
    "packages/solana_kit_attestation_service/CHANGELOG.md",
    "packages/solana_kit_codecs/CHANGELOG.md",
    "packages/solana_kit_codecs_core/CHANGELOG.md",
    "packages/solana_kit_codecs_data_structures/CHANGELOG.md",
    "packages/solana_kit_codecs_numbers/CHANGELOG.md",
    "packages/solana_kit_codecs_strings/CHANGELOG.md",
    "packages/solana_kit_compute_budget/CHANGELOG.md",
    "packages/solana_kit_config/CHANGELOG.md",
    "packages/solana_kit_dapp_publisher_cli/CHANGELOG.md",
    "packages/solana_kit_errors/CHANGELOG.md",
    "packages/solana_kit_fast_stable_stringify/CHANGELOG.md",
    "packages/solana_kit_fixed_points/CHANGELOG.md",
    "packages/solana_kit_helius/CHANGELOG.md",
    "packages/solana_kit_instruction_plans/CHANGELOG.md",
    "packages/solana_kit_instructions/CHANGELOG.md",
    "packages/solana_kit_integration_tests/CHANGELOG.md",
    "packages/solana_kit_jupiter/CHANGELOG.md",
    "packages/solana_kit_keys/CHANGELOG.md",
    "packages/solana_kit_lints/CHANGELOG.md",
    "packages/solana_kit_loader/CHANGELOG.md",
    "packages/solana_kit_memo/CHANGELOG.md",
    "packages/solana_kit_mobile_wallet_adapter/CHANGELOG.md",
    "packages/solana_kit_mobile_wallet_adapter_protocol/CHANGELOG.md",
    "packages/solana_kit_mpl_bubblegum/CHANGELOG.md",
    "packages/solana_kit_mpl_core/CHANGELOG.md",
    "packages/solana_kit_mpl_token_metadata/CHANGELOG.md",
    "packages/solana_kit_offchain_messages/CHANGELOG.md",
    "packages/solana_kit_options/CHANGELOG.md",
    "packages/solana_kit_program_client_core/CHANGELOG.md",
    "packages/solana_kit_programs/CHANGELOG.md",
    "packages/solana_kit_pyth/CHANGELOG.md",
    "packages/solana_kit_rpc/CHANGELOG.md",
    "packages/solana_kit_rpc_api/CHANGELOG.md",
    "packages/solana_kit_rpc_parsed_types/CHANGELOG.md",
    "packages/solana_kit_rpc_spec/CHANGELOG.md",
    "packages/solana_kit_rpc_spec_types/CHANGELOG.md",
    "packages/solana_kit_rpc_subscriptions/CHANGELOG.md",
    "packages/solana_kit_rpc_subscriptions_api/CHANGELOG.md",
    "packages/solana_kit_rpc_subscriptions_channel_websocket/CHANGELOG.md",
    "packages/solana_kit_rpc_transformers/CHANGELOG.md",
    "packages/solana_kit_rpc_transport_http/CHANGELOG.md",
    "packages/solana_kit_rpc_types/CHANGELOG.md",
    "packages/solana_kit_signers/CHANGELOG.md",
    "packages/solana_kit_sns/CHANGELOG.md",
    "packages/solana_kit_spl_account_compression/CHANGELOG.md",
    "packages/solana_kit_squads/CHANGELOG.md",
    "packages/solana_kit_stake/CHANGELOG.md",
    "packages/solana_kit_subscribable/CHANGELOG.md",
    "packages/solana_kit_subscriptions/CHANGELOG.md",
    "packages/solana_kit_surfpool/CHANGELOG.md",
    "packages/solana_kit_system/CHANGELOG.md",
    "packages/solana_kit_sysvars/CHANGELOG.md",
    "packages/solana_kit_test_matchers/CHANGELOG.md",
    "packages/solana_kit_token/CHANGELOG.md",
    "packages/solana_kit_token_2022/CHANGELOG.md",
    "packages/solana_kit_transaction_confirmation/CHANGELOG.md",
    "packages/solana_kit_transaction_introspection/CHANGELOG.md",
    "packages/solana_kit_transaction_messages/CHANGELOG.md",
    "packages/solana_kit_transactions/CHANGELOG.md",
    "packages/solana_kit_wallet_adapter/CHANGELOG.md",
    "packages/solana_kit_wallet_standard/CHANGELOG.md",
    "packages/solana_kit_wallet_ui/CHANGELOG.md"
  ],
  "deleted_changesets": [
    ".changeset/codama-renderer-toolchain.md",
    ".changeset/codama-renderers-dart-license-file.md",
    ".changeset/codecs-size-prefix-and-utf8-options.md",
    ".changeset/codecs-tap-and-256-bit.md",
    ".changeset/decoder-truncation-guards.md",
    ".changeset/error-code-number-parity.md",
    ".changeset/helius-await-sender-before-dispose.md",
    ".changeset/memo-v4-program-and-extraction-helpers.md",
    ".changeset/mutation-testing-tooling.md",
    ".changeset/raise-dart-flutter-baseline.md",
    ".changeset/remove-stubs-and-implement-real-behavior.md",
    ".changeset/restore-versions-table-inventory.md",
    ".changeset/sha256-differential-tests.md",
    ".changeset/speed-up-base-x-and-json-parsing.md",
    ".changeset/token-2022-js-v0-18-0.md",
    ".changeset/upstream-kit-v8-3-0.md",
    ".changeset/v1-transaction-test-coverage.md",
    ".changeset/wallet-adapter-analyzer-excludes.md",
    ".changeset/wallet-ui-skribble-presentation.md"
  ],
  "changesets": [
    {
      "path": ".changeset/codama-renderer-toolchain.md",
      "summary": "Update the renderer toolchain to TypeScript 7 and Vitest 5",
      "details": "The build and test toolchain moves to TypeScript 7.0.2, Vitest 5.0.0, `@vitest/coverage-v8` 5.0.0, and `@types/node` 26.5.1. Generated Dart output is unchanged; this is a tooling-only release.\n\nTypeScript 6 and later no longer discover `@types` packages implicitly under `moduleResolution: \"bundler\"`, and they require an explicit common source directory when emitting declarations. Both projects therefore declare the Node types and the declaration root directory:\n\n```jsonc\n// tsconfig.json\n{ \"compilerOptions\": { \"types\": [\"node\"] } }\n// tsconfig.declarations.json\n{ \"compilerOptions\": { \"rootDir\": \"./src\" } }\n```\n\nThe Vitest config also sets explicit `testTimeout` and `hookTimeout` budgets. Most tests in this package shell out to `dart format`, `dart analyze`, or `dart test`, so the 5s default was already marginal for the heaviest cases.",
      "targets": [
        {
          "id": "codama-renderers-dart",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "0d78dde5ec3f5c1971b8b3b7f97437b12f9d8e02",
            "short_sha": "0d78dde",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/0d78dde5ec3f5c1971b8b3b7f97437b12f9d8e02",
            "authored_at": "2026-09-13T21:04:56+01:00",
            "committed_at": "2026-09-13T21:04:56+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "0d78dde5ec3f5c1971b8b3b7f97437b12f9d8e02",
            "short_sha": "0d78dde",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/0d78dde5ec3f5c1971b8b3b7f97437b12f9d8e02",
            "authored_at": "2026-09-13T21:04:56+01:00",
            "committed_at": "2026-09-13T21:04:56+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/codama-renderers-dart-license-file.md",
      "summary": "Add the MIT LICENSE file to the renderer package",
      "details": "`packages/codama-renderers-dart` declared `\"license\": \"MIT\"` in its `package.json` but never shipped the license text itself. The file now matches the MIT LICENSE used by every other package in the workspace, satisfying the repo-wide package rule and making the license discoverable from a plain clone of the npm package.",
      "targets": [
        {
          "id": "codama-renderers-dart",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "4c8855bd608bb9f05324b001b552c9329679e441",
            "short_sha": "4c8855b",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441",
            "authored_at": "2026-09-19T13:03:12+01:00",
            "committed_at": "2026-09-19T13:03:12+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "4c8855bd608bb9f05324b001b552c9329679e441",
            "short_sha": "4c8855b",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441",
            "authored_at": "2026-09-19T13:03:12+01:00",
            "committed_at": "2026-09-19T13:03:12+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/codecs-size-prefix-and-utf8-options.md",
      "summary": "Add the upstream size-prefix and UTF-8 codec options",
      "details": "Ports the last two surfaces from upstream `@solana/kit` v8.3.0. Both are additive: no existing behavior changes.\n\n`requireSizePrefix` is a named parameter on `getArrayDecoder`, `getArrayCodec`, `getSetDecoder`, `getSetCodec`, `getMapDecoder`, and `getMapCodec`. Upstream defaults it to `false`, where an exhausted byte array decodes as an empty collection so a collection can be appended to an existing layout. This port defaults it to `true` and throws, because a silently empty collection hides truncated input. Pass `requireSizePrefix: false` to opt in:\n\n```dart\nfinal lenient = getArrayCodec(getU8Codec(), requireSizePrefix: false);\nlenient.decode(Uint8List(0)); // []\ngetArrayCodec(getU8Codec()).decode(Uint8List(0)); // throws\n```\n\n`Utf8CodecConfig` is accepted by `getUtf8Encoder`, `getUtf8Decoder`, and `getUtf8Codec`, and carries upstream's three options:\n\n- `fatal` rejects malformed input instead of replacing it. Upstream defaults to `false`, decoding bad bytes as `U+FFFD`; this port defaults to `true` and raises a `FormatException` from decoding and, for lone surrogates, from encoding.\n- `ignoreBOM` preserves a leading byte order mark. Both default to `false`, which strips it, matching Dart's `Utf8Decoder`.\n- `removeNullCharacters` strips `U+0000` from decoded strings. Upstream defaults to `true`; this port defaults to `false` so the decoded value reflects the bytes exactly.\n\nThe two divergent defaults exist so that malformed or null-padded account and instruction data cannot decode silently. To take upstream's behavior explicitly:\n\n```dart\nfinal codec = getUtf8Codec(\n  const Utf8CodecConfig(fatal: false, removeNullCharacters: true),\n);\n```",
      "targets": [
        {
          "id": "solana_kit_codecs_data_structures",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_codecs_strings",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "cf4ab873e32bb2ce2f4fe69e73fbc8aa6162894b",
            "short_sha": "cf4ab87",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/cf4ab873e32bb2ce2f4fe69e73fbc8aa6162894b",
            "authored_at": "2026-09-13T22:39:12+01:00",
            "committed_at": "2026-09-13T22:39:12+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "cf4ab873e32bb2ce2f4fe69e73fbc8aa6162894b",
            "short_sha": "cf4ab87",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/cf4ab873e32bb2ce2f4fe69e73fbc8aa6162894b",
            "authored_at": "2026-09-13T22:39:12+01:00",
            "committed_at": "2026-09-13T22:39:12+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/codecs-tap-and-256-bit.md",
      "summary": "Add 256-bit number codecs and tap codec helpers",
      "details": "Ports the two additive codec surfaces from upstream `@solana/kit` v8.3.0.\n\n`solana_kit_codecs_numbers` gains 256-bit integer codecs: `getU256Codec`, `getU256Encoder`, and `getU256Decoder` for unsigned values in `[0, 2^256 - 1]`, plus `getI256Codec`, `getI256Encoder`, and `getI256Decoder` for signed values in `[-(2^255), 2^255 - 1]`. Both serialize as 32 bytes, honour the `endian` option, and always decode to `BigInt`, matching the existing 64-bit and 128-bit codecs.\n\n`solana_kit_codecs_core` gains tap helpers that observe values or bytes without modifying them. Because any callback may throw, they double as validation guards that need no identity `transformEncoder`:\n\n```dart\nfinal guarded = tapDecoderBytes(getU8Decoder(), (bytes, offset) {\n  if (bytes[offset] > 1) throw StateError('Expected a 0 or a 1');\n});\n\nfinal spanned = tapEncoderBytes(getU8Encoder(), (bytes, pre, post) {\n  print('wrote ${post - pre} bytes at $pre');\n});\n```\n\n`tapEncoder`, `tapDecoder`, and `tapCodec` observe values; `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe bytes and offsets. Each wrapper preserves the size characteristics of the codec it wraps, so `FixedSizeEncoder` stays fixed-size and `VariableSizeEncoder` keeps its `maxSize`.\n\nNote that Dart's `Codec` is not an `Encoder` or a `Decoder`, so the value-level wrappers are typed against the encoder or decoder they observe; use `tapCodec` and `tapCodecBytes` to wrap a codec.",
      "targets": [
        {
          "id": "solana_kit_codecs_core",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_codecs_numbers",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "220066ecb27aa738fd787ac8ada3918540435cc1",
            "short_sha": "220066e",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1",
            "authored_at": "2026-09-13T21:05:30+01:00",
            "committed_at": "2026-09-13T21:05:30+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "220066ecb27aa738fd787ac8ada3918540435cc1",
            "short_sha": "220066e",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1",
            "authored_at": "2026-09-13T21:05:30+01:00",
            "committed_at": "2026-09-13T21:05:30+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/decoder-truncation-guards.md",
      "summary": "Reject truncated numeric reads with `SolanaError` instead of `RangeError`",
      "details": "A decoder reading a fixed-width field out of malformed wire data could raise a raw `RangeError` or `IndexError` out of the SDK instead of the documented `SolanaError`, so a caller catching `SolanaError` saw an escaped exception rather than a rejection it could handle. A truncated account, transaction, or RPC payload reaching the decoders was enough to trigger it; no signature or cluster access was required.\n\nThe guard that upstream `@solana/kit` applies in its number decoder factory was missing from three ports of it. `numberDecoderFactory` and `floatDecoderFactory` read through a `ByteData` view without first checking that the requested width was available, and the six `BigInt` decoders (`u64`, `i64`, `u128`, `i128`, `u256`, `i256`) indexed bytes directly with the same gap. All eight now call `assertByteArrayIsNotEmptyForCodec` and `assertByteArrayHasEnoughBytesForCodec` before reading, raising `codecsCannotDecodeEmptyByteArray` or `codecsInvalidByteLength` as upstream does. A new `bigIntDecoderFactory` carries the guard for the multi-word widths.\n\nTwo further escape sites in `solana_kit_transaction_messages` are fixed. The version 1 instruction payload slice computed `pos + numInstructionDataBytes` and sliced without confirming the buffer held that many bytes; it now asserts the length the way upstream's `fixDecoderSize` wrapper does. The transaction version decoder read `bytes[offset]` unguarded — upstream tolerates this because JavaScript yields `undefined`, which then silently takes the legacy branch and misreports a truncated buffer as an unversioned message, so this port rejects the empty buffer rather than reproducing that fallback.\n\nEncoders are unchanged and still raise `RangeError` when a destination buffer is too small. Upstream writes into a scratch buffer and then `bytes.set`s it, which throws in JavaScript too, so that behavior is deliberate parity rather than a defect.\n\nCallers that caught `RangeError` around a decode should catch `SolanaError`. Callers that already caught `SolanaError` now see malformed input rejected where it previously surfaced as a crash.",
      "targets": [
        {
          "id": "solana_kit_codecs_numbers",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_transaction_messages",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_transactions",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "f21cd21d26750d70f0c72e6acf8440346cf8175e",
            "short_sha": "f21cd21",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e",
            "authored_at": "2026-09-19T19:55:59+01:00",
            "committed_at": "2026-09-19T19:55:59+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "f21cd21d26750d70f0c72e6acf8440346cf8175e",
            "short_sha": "f21cd21",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e",
            "authored_at": "2026-09-19T19:55:59+01:00",
            "committed_at": "2026-09-19T19:55:59+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/error-code-number-parity.md",
      "summary": "Align error code numbers with upstream and report malformed UTF-8 as a code",
      "details": "`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.",
      "targets": [
        {
          "id": "solana_kit_errors",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_codecs_strings",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_memo",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_attestation_service",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_offchain_messages",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "a8f643f1ae7e6594fbfa972d473f7042b1f6ace0",
            "short_sha": "a8f643f",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0",
            "authored_at": "2026-09-14T15:41:35+01:00",
            "committed_at": "2026-09-14T15:41:35+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "a8f643f1ae7e6594fbfa972d473f7042b1f6ace0",
            "short_sha": "a8f643f",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0",
            "authored_at": "2026-09-14T15:41:35+01:00",
            "committed_at": "2026-09-14T15:41:35+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/helius-await-sender-before-dispose.md",
      "summary": "Await the sender service call before disposing the signing keypair",
      "details": "`buildTokenTransfer` now awaits `sendViaSender` inside the `try` block instead of returning the Future directly, so the ephemeral keypair is disposed after the send completes rather than at return time. Signing already finishes before the call, so this is a lifecycle hygiene change with no observable behavior difference; it satisfies the Dart 3.13 `unawaited_return_in_try_block` diagnostic that the Flutter 3.47 toolchain enables.",
      "targets": [
        {
          "id": "solana_kit_helius",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "e041e731bb0291af05c06c16d7042fdbf51166a1",
            "short_sha": "e041e73",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1",
            "authored_at": "2026-09-13T01:29:30+01:00",
            "committed_at": "2026-09-13T01:29:30+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "e041e731bb0291af05c06c16d7042fdbf51166a1",
            "short_sha": "e041e73",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1",
            "authored_at": "2026-09-13T01:29:30+01:00",
            "committed_at": "2026-09-13T01:29:30+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/memo-v4-program-and-extraction-helpers.md",
      "summary": "Track the memo v4 program and add memo extraction helpers",
      "details": "The workspace now tracks `solana-program/memo` at `js@v0.14.1` (previously `js@v0.13.1`). The `mpl-token-metadata` reference pin also moves to `353d01be4af3`; its IDL is byte-identical, so that package is unaffected.\n\n`solana_kit_address_constants` moves `memoProgramAddress` to the v4 memo program (`Memo4c2pN8afCj432Lb7RMVKi9PbQnnW7ewFFaV3oAH`), matching the upstream IDL `publicKey` as of `js@v0.14.0`. The previous v3 address stays available as the new `memoLegacyProgramAddressV3` constant, and `memoLegacyProgramAddress` (v1) is unchanged. Code that builds new memo instructions picks up the v4 program automatically; code that must keep targeting v3 names the legacy constant explicitly.\n\n`solana_kit_memo` ports the upstream extraction helpers from `js@v0.14.1`:\n\n- `getMemosFromInstructions` scans a list of instructions, matches every deployed Memo program address, and returns the UTF-8 decoded memo text with the raw bytes, source program address, and instruction index.\n- `ExtractedMemo` carries one extracted memo.\n- `supportedMemoProgramAddresses` lists every deployed Memo program address ordered from oldest (v1) to newest (v4).\n\nThe generated layer is regenerated against `js@v0.14.1` with the current renderer: the program page is byte-identical because the program address flows through the well-known constants, and the AddMemo data decoder now validates byte length strictly, throwing `SolanaError` with `codecsInvalidByteLength` on trailing bytes. `solana_kit_errors` moves from a dev dependency to a dependency because the generated decoder references it.\n\n`codama-renderers-dart` maps the v4 address to `memoProgramAddress` and the v3 address to `memoLegacyProgramAddressV3` in its well-known address registry, so regenerated clients re-export the canonical constants instead of hardcoding address strings.\n\nBuild new memo instructions against `memoProgramAddress` (v4). Where a memo must be executed by a program the runtime provides, check what is deployed: SurfPool, used by this repository's on-chain integration tests, ships the v1 and v3 programs as executable bytecode but resolves v4 to a placeholder, so those tests invoke `memoLegacyProgramAddressV3`. All three programs share the same instruction format, so only the program the transaction targets differs.",
      "targets": [
        {
          "id": "codama-renderers-dart",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_address_constants",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_integration_tests",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_memo",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "9dcb59a09c4b13fc471a286612da570a8141e3c7",
            "short_sha": "9dcb59a",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7",
            "authored_at": "2026-09-18T08:45:13+01:00",
            "committed_at": "2026-09-18T08:45:13+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "9dcb59a09c4b13fc471a286612da570a8141e3c7",
            "short_sha": "9dcb59a",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7",
            "authored_at": "2026-09-18T08:45:13+01:00",
            "committed_at": "2026-09-18T08:45:13+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/mutation-testing-tooling.md",
      "summary": "Add scoped mutation testing tooling",
      "details": "Adds a mutation testing setup built on `mutation_test`, so the suite can be checked for tests that assert current behavior rather than the intended contract. Coverage cannot detect that failure mode: a test asserting the wrong contract still executes every line.\n\nFour devenv tasks wrap a new driver at `scripts/run_mutation_testing.dart`:\n\n- `mutation:list` shows the configured scopes and their sizes.\n- `mutation:check` reports how completely each scope's test list covers its transitive dependents.\n- `mutation:changed` runs only the scopes touched since `origin/main`.\n- `mutation:run` runs a scope directly, with `--scope`, `--full`, and `--coverage` options.\n\nScopes live in `config/mutation/scopes.json` and pair source files with the test directories that can detect a change in them, covering the numeric codecs, transaction messages, transaction envelopes, string codecs, codec core, keys, PDAs, hashing, and the untrusted-input parsers.\n\nA default run executes only a scope's listed tests, which costs about two seconds per mutant. Because shared packages have many dependents (`solana_kit_codecs_numbers` has 58, `solana_kit_codecs_core` has 59), a survivor in code other packages exercise can be a false positive; `--full` expands the run to every dependent so no survivor is. `mutation:check` reports the size of that gap per scope.\n\n`config/mutation/rules.xml` replaces the builtin rule set, which is unsuitable for Dart: the builtin `<` rule also matches the first character of `<<` and the builtin argument rules reorder call arguments, so both produce mutations that fail to compile and are then reported as survivors. The rules here use lookarounds to match genuine comparisons, and exclude comments and string literals.\n\nThis is advisory tooling only. It is not wired into CI: equivalent mutants survive legitimately and would make a required check permanently red. See `docs/agents/mutation-testing.md`.",
      "targets": [
        {
          "id": "solana_kit",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "186ba3d9b982b64f6d32156d41f59465fc5770b2",
            "short_sha": "186ba3d",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/186ba3d9b982b64f6d32156d41f59465fc5770b2",
            "authored_at": "2026-09-19T21:20:50+01:00",
            "committed_at": "2026-09-19T21:20:50+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "186ba3d9b982b64f6d32156d41f59465fc5770b2",
            "short_sha": "186ba3d",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/186ba3d9b982b64f6d32156d41f59465fc5770b2",
            "authored_at": "2026-09-19T21:20:50+01:00",
            "committed_at": "2026-09-19T21:20:50+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/raise-dart-flutter-baseline.md",
      "summary": "Raise the Dart and Flutter baseline",
      "details": "The workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```",
      "targets": [
        {
          "id": "codama-renderers-dart",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_accounts",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_address",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_address_constants",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_address_lookup_table",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_addresses",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_anchor",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_associated_token_account",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_attestation_service",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_codecs",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_codecs_core",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_codecs_data_structures",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_codecs_numbers",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_codecs_strings",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_compute_budget",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_config",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_dapp_publisher_cli",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_errors",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_fast_stable_stringify",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_fixed_points",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_helius",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_instruction_plans",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_instructions",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_integration_tests",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_jupiter",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_keys",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_lints",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_loader",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_memo",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_mobile_wallet_adapter",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_mobile_wallet_adapter_protocol",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_mpl_bubblegum",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_mpl_core",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_mpl_token_metadata",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_offchain_messages",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_options",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_program_client_core",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_programs",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_pyth",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_rpc",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_rpc_api",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_rpc_parsed_types",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_rpc_spec",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_rpc_spec_types",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_rpc_subscriptions",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_rpc_subscriptions_api",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_rpc_subscriptions_channel_websocket",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_rpc_transformers",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_rpc_transport_http",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_rpc_types",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_signers",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_sns",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_spl_account_compression",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_squads",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_stake",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_subscribable",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_subscriptions",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_surfpool",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_system",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_sysvars",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_test_matchers",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_token",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_token_2022",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_transaction_confirmation",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_transaction_introspection",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_transaction_messages",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_transactions",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_wallet_adapter",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_wallet_standard",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        },
        {
          "id": "solana_kit_wallet_ui",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "5f5fe01f3e2220ccfee54cc26e82c3face26589d",
            "short_sha": "5f5fe01",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d",
            "authored_at": "2026-09-13T22:05:58+01:00",
            "committed_at": "2026-09-13T22:05:58+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "19932dba1979f1190b7501947b87eb8a4d4cc8d5",
            "short_sha": "19932db",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5",
            "authored_at": "2026-09-16T00:33:42+01:00",
            "committed_at": "2026-09-16T00:33:42+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/remove-stubs-and-implement-real-behavior.md",
      "summary": "Replace stubbed functions with real implementations",
      "details": "Several public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.",
      "targets": [
        {
          "id": "solana_kit",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_transaction_messages",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_rpc",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_addresses",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_helius",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_dapp_publisher_cli",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "19932dba1979f1190b7501947b87eb8a4d4cc8d5",
            "short_sha": "19932db",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5",
            "authored_at": "2026-09-16T00:33:42+01:00",
            "committed_at": "2026-09-16T00:33:42+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "19932dba1979f1190b7501947b87eb8a4d4cc8d5",
            "short_sha": "19932db",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5",
            "authored_at": "2026-09-16T00:33:42+01:00",
            "committed_at": "2026-09-16T00:33:42+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/restore-versions-table-inventory.md",
      "summary": "Restore complete version inventories in package READMEs",
      "details": "The `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```",
      "targets": [
        {
          "id": "solana_kit_anchor",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_attestation_service",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_jupiter",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_mpl_core",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_mpl_token_metadata",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_pyth",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_sns",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_squads",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "4c8855bd608bb9f05324b001b552c9329679e441",
            "short_sha": "4c8855b",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441",
            "authored_at": "2026-09-19T13:03:12+01:00",
            "committed_at": "2026-09-19T13:03:12+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "4c8855bd608bb9f05324b001b552c9329679e441",
            "short_sha": "4c8855b",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441",
            "authored_at": "2026-09-19T13:03:12+01:00",
            "committed_at": "2026-09-19T13:03:12+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/sha256-differential-tests.md",
      "summary": "Verify both hand-written SHA-256 implementations against an independent hash",
      "details": "`solana_kit_anchor` and `solana_kit_sns` each carry their own pure-Dart SHA-256. Anchor uses it to derive program discriminators and SNS to derive name account addresses, so a defect in either changes which instruction or account a program resolves to — a wrong result rather than an exception. Both were covered only by a handful of fixed NIST vectors, which pin the round function but rarely land on the padding and block boundaries where a length bug hides.\n\nEach package now has a `sha256_differential_test.dart` comparing its implementation against `package:crypto` (the same dependency `solana_kit_addresses` already uses). Coverage includes every input length from 0 to 200, which crosses the 55/56-byte padding transitions and the 64-byte block boundary, lengths from 255 through 10000 for multi-block behavior, randomized inputs, and all-zero and all-`0xff` buffers. Anchor additionally checks `instructionDiscriminator` against the truncated namespaced digest, and SNS checks `getHashedName` against `sha256(\"SPL Name Service\" + name)`.\n\nBoth implementations agree with the reference on every case. No library behavior changes.",
      "targets": [
        {
          "id": "solana_kit_anchor",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_sns",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "f21cd21d26750d70f0c72e6acf8440346cf8175e",
            "short_sha": "f21cd21",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e",
            "authored_at": "2026-09-19T19:55:59+01:00",
            "committed_at": "2026-09-19T19:55:59+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "f21cd21d26750d70f0c72e6acf8440346cf8175e",
            "short_sha": "f21cd21",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e",
            "authored_at": "2026-09-19T19:55:59+01:00",
            "committed_at": "2026-09-19T19:55:59+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/speed-up-base-x-and-json-parsing.md",
      "summary": "Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing",
      "details": "Same results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.",
      "targets": [
        {
          "id": "solana_kit_addresses",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_codecs_strings",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_rpc_spec_types",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_fixed_points",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_transaction_messages",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "b22257e1d1cb146eda742cd3da352b6a90a49f1d",
            "short_sha": "b22257e",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d",
            "authored_at": "2026-09-19T15:38:11+01:00",
            "committed_at": "2026-09-19T15:38:11+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "b22257e1d1cb146eda742cd3da352b6a90a49f1d",
            "short_sha": "b22257e",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d",
            "authored_at": "2026-09-19T15:38:11+01:00",
            "committed_at": "2026-09-19T15:38:11+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/token-2022-js-v0-18-0.md",
      "summary": "Track Token-2022 js@v0.18.0",
      "details": "`solana_kit_token_2022` follows `solana-program/token-2022` to `js@v0.18.0`, up from `js@v0.16.1`. The pin had been held back because the migration needed handwritten-layer work; that work is now done.\n\nThe generated extension union renames its variants to the renderer's current convention, prefixing each with the union name: `TransferFeeConfig` becomes `ExtensionTransferFeeConfig`, `MetadataPointer` becomes `ExtensionMetadataPointer`, and so on for all twenty-nine variants. The prefix keeps variants readable at the call site and stops them from colliding with same-named generated types. Update pattern matches and constructors:\n\n```dart\nfinal instructions = getPreInitializeInstructionsForMintExtensions(\n  mint: mintAddress,\n  extensions: [\n    ExtensionTransferFeeConfig(\n      transferFeeConfigAuthority: authority,\n      withdrawWithheldAuthority: authority,\n      withheldAmount: BigInt.zero,\n      olderTransferFee: olderFee,\n      newerTransferFee: newerFee,\n    ),\n  ],\n);\n```\n\nMint and token accounts now decode their TLV extension region the way the program reads it. The previous codec decoded the region as a `remainder` array, which had to consume every trailing byte as an entry, so an account allocated with unused space either threw or reported the padding as a spurious `Uninitialized` extension. The new codec walks entries until it meets an `Uninitialized` (type 0) header or fewer than two bytes remain, and ignores whatever follows, matching the program and `@solana/spl-token`:\n\n```dart\nfinal account = getMintDecoder().decode(bytesWithTwoBytesOfPadding);\n// extensions now holds only the real entries, never the padding.\n```\n\n`solana_kit_address_constants`-style address handling is unchanged; `getMintSize` and `getTokenSize` keep their existing signatures and results, and now share the same encoder the accounts use, so a size computed from a list of extensions always matches what the encoder writes.\n\n`codama-renderers-dart` gains `linkOverrides`, which lets an IDL link be backed by a hand-written Dart codec while the wrappers around it (`Option`, `HiddenPrefix`, …) still come from the IDL. That is what the Token-2022 extension region uses, and it is the extension point for any future type Codama cannot express. The renderer also stops requiring an account to match its size discriminator exactly: a size discriminator describes an account's fixed prefix, so accounts with variable trailing fields are legitimately longer. Instruction sizes are still matched exactly, since there the size identifies the whole payload.",
      "targets": [
        {
          "id": "codama-renderers-dart",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_token_2022",
          "kind": "package",
          "bump": "major",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "major"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "9dcb59a09c4b13fc471a286612da570a8141e3c7",
            "short_sha": "9dcb59a",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7",
            "authored_at": "2026-09-18T08:45:13+01:00",
            "committed_at": "2026-09-18T08:45:13+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "9dcb59a09c4b13fc471a286612da570a8141e3c7",
            "short_sha": "9dcb59a",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7",
            "authored_at": "2026-09-18T08:45:13+01:00",
            "committed_at": "2026-09-18T08:45:13+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/upstream-kit-v8-3-0.md",
      "summary": "Track @solana/kit v8.3.0",
      "details": "The workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`",
      "targets": [
        {
          "id": "solana_kit",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_codecs_core",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_codecs_numbers",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        },
        {
          "id": "solana_kit_errors",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "220066ecb27aa738fd787ac8ada3918540435cc1",
            "short_sha": "220066e",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1",
            "authored_at": "2026-09-13T21:05:30+01:00",
            "committed_at": "2026-09-13T21:05:30+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "220066ecb27aa738fd787ac8ada3918540435cc1",
            "short_sha": "220066e",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1",
            "authored_at": "2026-09-13T21:05:30+01:00",
            "committed_at": "2026-09-13T21:05:30+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/v1-transaction-test-coverage.md",
      "summary": "Add version 1 transaction coverage with oversized payloads",
      "details": "Adds regression tests for the version 1 transaction wire format introduced by SIMD-0296 and SIMD-0385, which raise the transaction size ceiling from 1232 to 4096 bytes.\n\n`packages/solana_kit_transactions/test/v1_transaction_test.dart` pins the exact bytes against vectors generated by upstream `@solana/kit` 8.3.0, so a regression in the v1 envelope, config mask, or config value encoding fails loudly instead of silently producing transactions the cluster rejects. The vectors live in `test/fixtures/v1_wire_vectors.json`, alongside a README documenting how to regenerate them; the largest asserts a 3216-byte transaction byte-for-byte, which no legacy transaction can represent.\n\nOn-chain coverage lands in `packages/solana_kit_integration_tests/test/integration/v1_transaction_test.dart`. It submits a v1 transaction with a 1600-byte memo — over the legacy ceiling — and asserts the node accepts it, recovers the full memo, reports the inline `transactionConfig` with the requested compute unit and loaded accounts limits, and moves lamports. The same suite asserts the node rejects a payload above 4096 bytes.\n\nA second on-chain suite, `resource_limit_estimation_test.dart`, submits a v1 transaction whose limits were produced by `estimateResourceLimitsFactory` and asserts the node executes it.\n\n`IntegrationTestEnv` gains `sendV1Instructions` and `buildV1WireTransaction` helpers, and `fetchTransaction` now requests `maxSupportedTransactionVersion: 1` so v1 responses can be read back rather than failing with RPC error `-32015`. No public library behavior changes.",
      "targets": [
        {
          "id": "solana_kit_transactions",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "19932dba1979f1190b7501947b87eb8a4d4cc8d5",
            "short_sha": "19932db",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5",
            "authored_at": "2026-09-16T00:33:42+01:00",
            "committed_at": "2026-09-16T00:33:42+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "19932dba1979f1190b7501947b87eb8a4d4cc8d5",
            "short_sha": "19932db",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5",
            "authored_at": "2026-09-16T00:33:42+01:00",
            "committed_at": "2026-09-16T00:33:42+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/wallet-adapter-analyzer-excludes.md",
      "summary": "Refresh analyzer excludes for the Flutter 3.47 toolchain",
      "details": "The Flutter 3.47 tooling rewrites package `analysis_options.yaml` files during resolution to exclude generated `build/**` output from analysis. This applies the same exclusion to the wallet adapter and mobile wallet adapter packages (and the mobile example), keeping their analysis options stable under the updated FVM pin. No public API or behavior changes.",
      "targets": [
        {
          "id": "solana_kit_wallet_adapter",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        },
        {
          "id": "solana_kit_mobile_wallet_adapter",
          "kind": "package",
          "bump": "patch",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "patch"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "e041e731bb0291af05c06c16d7042fdbf51166a1",
            "short_sha": "e041e73",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1",
            "authored_at": "2026-09-13T01:29:30+01:00",
            "committed_at": "2026-09-13T01:29:30+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "e041e731bb0291af05c06c16d7042fdbf51166a1",
            "short_sha": "e041e73",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1",
            "authored_at": "2026-09-13T01:29:30+01:00",
            "committed_at": "2026-09-13T01:29:30+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    },
    {
      "path": ".changeset/wallet-ui-skribble-presentation.md",
      "summary": "Add a hand-drawn Skribble wallet presentation",
      "details": "The wallet components can now render with [skribble](https://pub.dev/packages/skribble), the hand-drawn Flutter design system. `SkribbleWalletButton` mirrors the Material and Cupertino connect buttons — connect, progress, connected-account, and disconnect states — using a `WiredFilledButton` and a hand-drawn disconnect sheet, and `showSkribbleWalletPicker` opens the picker as a `WiredBottomSheet` on compact viewports or a `WiredDialog` on wide ones, with rough-inked wallet tiles and a doodle-marked empty state over the same `WalletPickerContent` behavior as the other presentations.\n\n`skribbleWalletPalette` maps a `WiredThemeData` onto the framework-neutral `WalletUiPalette` for applications that embed `WalletPickerContent` in their own hand-drawn shell, and `resolveSkribbleWalletTheme` resolves the effective theme (explicit override, then nearest `WiredTheme`, then skribble's default ink). The example app — which is also the embedded docs demo — gained a presentation selector covering Adaptive, Material, Cupertino, and Skribble so the hand-drawn wallet can be compared live.\n\nSkribble is a direct dependency, so the package's Flutter floor rises from 3.44 to 3.47 and the workspace FVM pin moves to 3.47.4 accordingly.\n\n```dart\nSkribbleWalletButton(\n  controller: walletController,\n  theme: WiredThemeData.cuddly(),\n);\n```",
      "targets": [
        {
          "id": "solana_kit_wallet_ui",
          "kind": "package",
          "bump": "minor",
          "origin": "direct-change",
          "evidence_refs": [],
          "change_type": "minor"
        }
      ],
      "context": {
        "provider": "github",
        "host": "github.com",
        "capabilities": {
          "commit_web_urls": true,
          "actor_profiles": true,
          "review_request_lookup": true,
          "related_issues": true,
          "issue_comments": true
        },
        "introduced": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "e041e731bb0291af05c06c16d7042fdbf51166a1",
            "short_sha": "e041e73",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1",
            "authored_at": "2026-09-13T01:29:30+01:00",
            "committed_at": "2026-09-13T01:29:30+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "last_updated": {
          "actor": {
            "provider": "github",
            "host": "github.com",
            "id": null,
            "login": null,
            "display_name": "Ifiok Jr.",
            "url": null,
            "source": "commit_author"
          },
          "commit": {
            "provider": "github",
            "host": "github.com",
            "sha": "e041e731bb0291af05c06c16d7042fdbf51166a1",
            "short_sha": "e041e73",
            "url": "https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1",
            "authored_at": "2026-09-13T01:29:30+01:00",
            "committed_at": "2026-09-13T01:29:30+01:00",
            "author_name": "Ifiok Jr.",
            "author_email": "1160934+ifiokjr@users.noreply.github.com"
          },
          "review_request": null
        },
        "related_issues": []
      }
    }
  ],
  "changelogs": [
    {
      "owner_id": "main",
      "owner_kind": "group",
      "output": "default",
      "stream": "default",
      "path": "CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [
          "Grouped release for `main`."
        ],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\n_Packages:_ _solana_kit_, _solana_kit_accounts_, _solana_kit_address_, _solana_kit_anchor_, _solana_kit_address_constants_, _solana_kit_addresses_, _solana_kit_codecs_, _solana_kit_codecs_core_, _solana_kit_codecs_data_structures_, _solana_kit_codecs_numbers_, _solana_kit_codecs_strings_, _solana_kit_fixed_points_, _solana_kit_errors_, _solana_kit_fast_stable_stringify_, _solana_kit_jupiter_, _solana_kit_instruction_plans_, _solana_kit_instructions_, _solana_kit_keys_, _solana_kit_lints_, _solana_kit_mpl_core_, _solana_kit_mpl_token_metadata_, _solana_kit_options_, _solana_kit_pyth_, _solana_kit_program_client_core_, _solana_kit_programs_, _solana_kit_rpc_, _solana_kit_rpc_api_, _solana_kit_rpc_parsed_types_, _solana_kit_rpc_spec_, _solana_kit_rpc_spec_types_, _solana_kit_rpc_subscriptions_, _solana_kit_rpc_subscriptions_api_, _solana_kit_rpc_subscriptions_channel_websocket_, _solana_kit_rpc_transformers_, _solana_kit_rpc_transport_http_, _solana_kit_rpc_types_, _solana_kit_signers_, _solana_kit_sns_, _solana_kit_squads_, _solana_kit_subscribable_, _solana_kit_transaction_confirmation_, _solana_kit_transaction_introspection_, _solana_kit_transaction_messages_, _solana_kit_transactions_\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)",
              "#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n_Packages:_ _solana_kit_errors_\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Replace stubbed functions with real implementations\n\n_Packages:_ _solana_kit_, _solana_kit_addresses_, _solana_kit_rpc_, _solana_kit_transaction_messages_\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)",
              "#### Track @solana/kit v8.3.0\n\n_Packages:_ _solana_kit_, _solana_kit_codecs_core_, _solana_kit_codecs_numbers_\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)",
              "#### Track the memo v4 program and add memo extraction helpers\n\n_Packages:_ _solana_kit_address_constants_\n\nThe workspace now tracks `solana-program/memo` at `js@v0.14.1` (previously `js@v0.13.1`). The `mpl-token-metadata` reference pin also moves to `353d01be4af3`; its IDL is byte-identical, so that package is unaffected.\n\n`solana_kit_address_constants` moves `memoProgramAddress` to the v4 memo program (`Memo4c2pN8afCj432Lb7RMVKi9PbQnnW7ewFFaV3oAH`), matching the upstream IDL `publicKey` as of `js@v0.14.0`. The previous v3 address stays available as the new `memoLegacyProgramAddressV3` constant, and `memoLegacyProgramAddress` (v1) is unchanged. Code that builds new memo instructions picks up the v4 program automatically; code that must keep targeting v3 names the legacy constant explicitly.\n\n`solana_kit_memo` ports the upstream extraction helpers from `js@v0.14.1`:\n\n- `getMemosFromInstructions` scans a list of instructions, matches every deployed Memo program address, and returns the UTF-8 decoded memo text with the raw bytes, source program address, and instruction index.\n- `ExtractedMemo` carries one extracted memo.\n- `supportedMemoProgramAddresses` lists every deployed Memo program address ordered from oldest (v1) to newest (v4).\n\nThe generated layer is regenerated against `js@v0.14.1` with the current renderer: the program page is byte-identical because the program address flows through the well-known constants, and the AddMemo data decoder now validates byte length strictly, throwing `SolanaError` with `codecsInvalidByteLength` on trailing bytes. `solana_kit_errors` moves from a dev dependency to a dependency because the generated decoder references it.\n\n`codama-renderers-dart` maps the v4 address to `memoProgramAddress` and the v3 address to `memoLegacyProgramAddressV3` in its well-known address registry, so regenerated clients re-export the canonical constants instead of hardcoding address strings.\n\nBuild new memo instructions against `memoProgramAddress` (v4). Where a memo must be executed by a program the runtime provides, check what is deployed: SurfPool, used by this repository's on-chain integration tests, ships the v1 and v3 programs as executable bytecode but resolves v4 to a placeholder, so those tests invoke `memoLegacyProgramAddressV3`. All three programs share the same instruction format, so only the program the transaction targets differs.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)",
              "#### Add 256-bit number codecs and tap codec helpers\n\n_Packages:_ _solana_kit_codecs_core_, _solana_kit_codecs_numbers_\n\nPorts the two additive codec surfaces from upstream `@solana/kit` v8.3.0.\n\n`solana_kit_codecs_numbers` gains 256-bit integer codecs: `getU256Codec`, `getU256Encoder`, and `getU256Decoder` for unsigned values in `[0, 2^256 - 1]`, plus `getI256Codec`, `getI256Encoder`, and `getI256Decoder` for signed values in `[-(2^255), 2^255 - 1]`. Both serialize as 32 bytes, honour the `endian` option, and always decode to `BigInt`, matching the existing 64-bit and 128-bit codecs.\n\n`solana_kit_codecs_core` gains tap helpers that observe values or bytes without modifying them. Because any callback may throw, they double as validation guards that need no identity `transformEncoder`:\n\n```dart\nfinal guarded = tapDecoderBytes(getU8Decoder(), (bytes, offset) {\n  if (bytes[offset] > 1) throw StateError('Expected a 0 or a 1');\n});\n\nfinal spanned = tapEncoderBytes(getU8Encoder(), (bytes, pre, post) {\n  print('wrote ${post - pre} bytes at $pre');\n});\n```\n\n`tapEncoder`, `tapDecoder`, and `tapCodec` observe values; `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe bytes and offsets. Each wrapper preserves the size characteristics of the codec it wraps, so `FixedSizeEncoder` stays fixed-size and `VariableSizeEncoder` keeps its `maxSize`.\n\nNote that Dart's `Codec` is not an `Encoder` or a `Decoder`, so the value-level wrappers are typed against the encoder or decoder they observe; use `tapCodec` and `tapCodecBytes` to wrap a codec.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)",
              "#### Add the upstream size-prefix and UTF-8 codec options\n\n_Packages:_ _solana_kit_codecs_data_structures_, _solana_kit_codecs_strings_\n\nPorts the last two surfaces from upstream `@solana/kit` v8.3.0. Both are additive: no existing behavior changes.\n\n`requireSizePrefix` is a named parameter on `getArrayDecoder`, `getArrayCodec`, `getSetDecoder`, `getSetCodec`, `getMapDecoder`, and `getMapCodec`. Upstream defaults it to `false`, where an exhausted byte array decodes as an empty collection so a collection can be appended to an existing layout. This port defaults it to `true` and throws, because a silently empty collection hides truncated input. Pass `requireSizePrefix: false` to opt in:\n\n```dart\nfinal lenient = getArrayCodec(getU8Codec(), requireSizePrefix: false);\nlenient.decode(Uint8List(0)); // []\ngetArrayCodec(getU8Codec()).decode(Uint8List(0)); // throws\n```\n\n`Utf8CodecConfig` is accepted by `getUtf8Encoder`, `getUtf8Decoder`, and `getUtf8Codec`, and carries upstream's three options:\n\n- `fatal` rejects malformed input instead of replacing it. Upstream defaults to `false`, decoding bad bytes as `U+FFFD`; this port defaults to `true` and raises a `FormatException` from decoding and, for lone surrogates, from encoding.\n- `ignoreBOM` preserves a leading byte order mark. Both default to `false`, which strips it, matching Dart's `Utf8Decoder`.\n- `removeNullCharacters` strips `U+0000` from decoded strings. Upstream defaults to `true`; this port defaults to `false` so the decoded value reflects the bytes exactly.\n\nThe two divergent defaults exist so that malformed or null-padded account and instruction data cannot decode silently. To take upstream's behavior explicitly:\n\n```dart\nfinal codec = getUtf8Codec(\n  const Utf8CodecConfig(fatal: false, removeNullCharacters: true),\n);\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [cf4ab87](https://github.com/openbudgetfun/solana_kit/commit/cf4ab873e32bb2ce2f4fe69e73fbc8aa6162894b)",
              "#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n_Packages:_ _solana_kit_codecs_strings_\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Add scoped mutation testing tooling\n\n_Packages:_ _solana_kit_\n\nAdds a mutation testing setup built on `mutation_test`, so the suite can be checked for tests that assert current behavior rather than the intended contract. Coverage cannot detect that failure mode: a test asserting the wrong contract still executes every line.\n\nFour devenv tasks wrap a new driver at `scripts/run_mutation_testing.dart`:\n\n- `mutation:list` shows the configured scopes and their sizes.\n- `mutation:check` reports how completely each scope's test list covers its transitive dependents.\n- `mutation:changed` runs only the scopes touched since `origin/main`.\n- `mutation:run` runs a scope directly, with `--scope`, `--full`, and `--coverage` options.\n\nScopes live in `config/mutation/scopes.json` and pair source files with the test directories that can detect a change in them, covering the numeric codecs, transaction messages, transaction envelopes, string codecs, codec core, keys, PDAs, hashing, and the untrusted-input parsers.\n\nA default run executes only a scope's listed tests, which costs about two seconds per mutant. Because shared packages have many dependents (`solana_kit_codecs_numbers` has 58, `solana_kit_codecs_core` has 59), a survivor in code other packages exercise can be a false positive; `--full` expands the run to every dependent so no survivor is. `mutation:check` reports the size of that gap per scope.\n\n`config/mutation/rules.xml` replaces the builtin rule set, which is unsuitable for Dart: the builtin `<` rule also matches the first character of `<<` and the builtin argument rules reorder call arguments, so both produce mutations that fail to compile and are then reported as survivors. The rules here use lookarounds to match genuine comparisons, and exclude comments and string literals.\n\nThis is advisory tooling only. It is not wired into CI: equivalent mutants survive legitimately and would make a required check permanently red. See `docs/agents/mutation-testing.md`.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [186ba3d](https://github.com/openbudgetfun/solana_kit/commit/186ba3d9b982b64f6d32156d41f59465fc5770b2)",
              "#### Restore complete version inventories in package READMEs\n\n_Packages:_ _solana_kit_anchor_, _solana_kit_jupiter_, _solana_kit_mpl_core_, _solana_kit_mpl_token_metadata_, _solana_kit_pyth_, _solana_kit_sns_, _solana_kit_squads_\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)",
              "#### Verify both hand-written SHA-256 implementations against an independent hash\n\n_Packages:_ _solana_kit_anchor_, _solana_kit_sns_\n\n`solana_kit_anchor` and `solana_kit_sns` each carry their own pure-Dart SHA-256. Anchor uses it to derive program discriminators and SNS to derive name account addresses, so a defect in either changes which instruction or account a program resolves to — a wrong result rather than an exception. Both were covered only by a handful of fixed NIST vectors, which pin the round function but rarely land on the padding and block boundaries where a length bug hides.\n\nEach package now has a `sha256_differential_test.dart` comparing its implementation against `package:crypto` (the same dependency `solana_kit_addresses` already uses). Coverage includes every input length from 0 to 200, which crosses the 55/56-byte padding transitions and the 64-byte block boundary, lengths from 255 through 10000 for multi-block behavior, randomized inputs, and all-zero and all-`0xff` buffers. Anchor additionally checks `instructionDiscriminator` against the truncated namespaced digest, and SNS checks `getHashedName` against `sha256(\"SPL Name Service\" + name)`.\n\nBoth implementations agree with the reference on every case. No library behavior changes.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)",
              "#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\n_Packages:_ _solana_kit_addresses_, _solana_kit_codecs_strings_, _solana_kit_fixed_points_, _solana_kit_rpc_spec_types_, _solana_kit_transaction_messages_\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)",
              "#### Reject truncated numeric reads with `SolanaError` instead of `RangeError`\n\n_Packages:_ _solana_kit_codecs_numbers_, _solana_kit_transaction_messages_, _solana_kit_transactions_\n\nA decoder reading a fixed-width field out of malformed wire data could raise a raw `RangeError` or `IndexError` out of the SDK instead of the documented `SolanaError`, so a caller catching `SolanaError` saw an escaped exception rather than a rejection it could handle. A truncated account, transaction, or RPC payload reaching the decoders was enough to trigger it; no signature or cluster access was required.\n\nThe guard that upstream `@solana/kit` applies in its number decoder factory was missing from three ports of it. `numberDecoderFactory` and `floatDecoderFactory` read through a `ByteData` view without first checking that the requested width was available, and the six `BigInt` decoders (`u64`, `i64`, `u128`, `i128`, `u256`, `i256`) indexed bytes directly with the same gap. All eight now call `assertByteArrayIsNotEmptyForCodec` and `assertByteArrayHasEnoughBytesForCodec` before reading, raising `codecsCannotDecodeEmptyByteArray` or `codecsInvalidByteLength` as upstream does. A new `bigIntDecoderFactory` carries the guard for the multi-word widths.\n\nTwo further escape sites in `solana_kit_transaction_messages` are fixed. The version 1 instruction payload slice computed `pos + numInstructionDataBytes` and sliced without confirming the buffer held that many bytes; it now asserts the length the way upstream's `fixDecoderSize` wrapper does. The transaction version decoder read `bytes[offset]` unguarded — upstream tolerates this because JavaScript yields `undefined`, which then silently takes the legacy branch and misreports a truncated buffer as an unversioned message, so this port rejects the empty buffer rather than reproducing that fallback.\n\nEncoders are unchanged and still raise `RangeError` when a destination buffer is too small. Upstream writes into a scratch buffer and then `bytes.set`s it, which throws in JavaScript too, so that behavior is deliberate parity rather than a defect.\n\nCallers that caught `RangeError` around a decode should catch `SolanaError`. Callers that already caught `SolanaError` now see malformed input rejected where it previously surfaced as a crash.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)",
              "#### Track @solana/kit v8.3.0\n\n_Packages:_ _solana_kit_errors_\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)",
              "#### Raise the Dart and Flutter baseline\n\n_Packages:_ _solana_kit_test_matchers_\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)",
              "#### Add version 1 transaction coverage with oversized payloads\n\n_Packages:_ _solana_kit_transactions_\n\nAdds regression tests for the version 1 transaction wire format introduced by SIMD-0296 and SIMD-0385, which raise the transaction size ceiling from 1232 to 4096 bytes.\n\n`packages/solana_kit_transactions/test/v1_transaction_test.dart` pins the exact bytes against vectors generated by upstream `@solana/kit` 8.3.0, so a regression in the v1 envelope, config mask, or config value encoding fails loudly instead of silently producing transactions the cluster rejects. The vectors live in `test/fixtures/v1_wire_vectors.json`, alongside a README documenting how to regenerate them; the largest asserts a 3216-byte transaction byte-for-byte, which no legacy transaction can represent.\n\nOn-chain coverage lands in `packages/solana_kit_integration_tests/test/integration/v1_transaction_test.dart`. It submits a v1 transaction with a 1600-byte memo — over the legacy ceiling — and asserts the node accepts it, recovers the full memo, reports the inline `transactionConfig` with the requested compute unit and loaded accounts limits, and moves lamports. The same suite asserts the node rejects a payload above 4096 bytes.\n\nA second on-chain suite, `resource_limit_estimation_test.dart`, submits a v1 transaction whose limits were produced by `estimateResourceLimitsFactory` and asserts the node executes it.\n\n`IntegrationTestEnv` gains `sendV1Instructions` and `buildV1WireTransaction` helpers, and `fetchTransaction` now requests `maxSupportedTransactionVersion: 1` so v1 responses can be read back rather than failing with RPC error `-32015`. No public library behavior changes.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\nGrouped release for `main`.\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\n_Packages:_ _solana_kit_, _solana_kit_accounts_, _solana_kit_address_, _solana_kit_anchor_, _solana_kit_address_constants_, _solana_kit_addresses_, _solana_kit_codecs_, _solana_kit_codecs_core_, _solana_kit_codecs_data_structures_, _solana_kit_codecs_numbers_, _solana_kit_codecs_strings_, _solana_kit_fixed_points_, _solana_kit_errors_, _solana_kit_fast_stable_stringify_, _solana_kit_jupiter_, _solana_kit_instruction_plans_, _solana_kit_instructions_, _solana_kit_keys_, _solana_kit_lints_, _solana_kit_mpl_core_, _solana_kit_mpl_token_metadata_, _solana_kit_options_, _solana_kit_pyth_, _solana_kit_program_client_core_, _solana_kit_programs_, _solana_kit_rpc_, _solana_kit_rpc_api_, _solana_kit_rpc_parsed_types_, _solana_kit_rpc_spec_, _solana_kit_rpc_spec_types_, _solana_kit_rpc_subscriptions_, _solana_kit_rpc_subscriptions_api_, _solana_kit_rpc_subscriptions_channel_websocket_, _solana_kit_rpc_transformers_, _solana_kit_rpc_transport_http_, _solana_kit_rpc_types_, _solana_kit_signers_, _solana_kit_sns_, _solana_kit_squads_, _solana_kit_subscribable_, _solana_kit_transaction_confirmation_, _solana_kit_transaction_introspection_, _solana_kit_transaction_messages_, _solana_kit_transactions_\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n_Packages:_ _solana_kit_errors_\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)\n\n### Features\n\n#### Replace stubbed functions with real implementations\n\n_Packages:_ _solana_kit_, _solana_kit_addresses_, _solana_kit_rpc_, _solana_kit_transaction_messages_\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n#### Track @solana/kit v8.3.0\n\n_Packages:_ _solana_kit_, _solana_kit_codecs_core_, _solana_kit_codecs_numbers_\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)\n\n#### Track the memo v4 program and add memo extraction helpers\n\n_Packages:_ _solana_kit_address_constants_\n\nThe workspace now tracks `solana-program/memo` at `js@v0.14.1` (previously `js@v0.13.1`). The `mpl-token-metadata` reference pin also moves to `353d01be4af3`; its IDL is byte-identical, so that package is unaffected.\n\n`solana_kit_address_constants` moves `memoProgramAddress` to the v4 memo program (`Memo4c2pN8afCj432Lb7RMVKi9PbQnnW7ewFFaV3oAH`), matching the upstream IDL `publicKey` as of `js@v0.14.0`. The previous v3 address stays available as the new `memoLegacyProgramAddressV3` constant, and `memoLegacyProgramAddress` (v1) is unchanged. Code that builds new memo instructions picks up the v4 program automatically; code that must keep targeting v3 names the legacy constant explicitly.\n\n`solana_kit_memo` ports the upstream extraction helpers from `js@v0.14.1`:\n\n- `getMemosFromInstructions` scans a list of instructions, matches every deployed Memo program address, and returns the UTF-8 decoded memo text with the raw bytes, source program address, and instruction index.\n- `ExtractedMemo` carries one extracted memo.\n- `supportedMemoProgramAddresses` lists every deployed Memo program address ordered from oldest (v1) to newest (v4).\n\nThe generated layer is regenerated against `js@v0.14.1` with the current renderer: the program page is byte-identical because the program address flows through the well-known constants, and the AddMemo data decoder now validates byte length strictly, throwing `SolanaError` with `codecsInvalidByteLength` on trailing bytes. `solana_kit_errors` moves from a dev dependency to a dependency because the generated decoder references it.\n\n`codama-renderers-dart` maps the v4 address to `memoProgramAddress` and the v3 address to `memoLegacyProgramAddressV3` in its well-known address registry, so regenerated clients re-export the canonical constants instead of hardcoding address strings.\n\nBuild new memo instructions against `memoProgramAddress` (v4). Where a memo must be executed by a program the runtime provides, check what is deployed: SurfPool, used by this repository's on-chain integration tests, ships the v1 and v3 programs as executable bytecode but resolves v4 to a placeholder, so those tests invoke `memoLegacyProgramAddressV3`. All three programs share the same instruction format, so only the program the transaction targets differs.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)\n\n#### Add 256-bit number codecs and tap codec helpers\n\n_Packages:_ _solana_kit_codecs_core_, _solana_kit_codecs_numbers_\n\nPorts the two additive codec surfaces from upstream `@solana/kit` v8.3.0.\n\n`solana_kit_codecs_numbers` gains 256-bit integer codecs: `getU256Codec`, `getU256Encoder`, and `getU256Decoder` for unsigned values in `[0, 2^256 - 1]`, plus `getI256Codec`, `getI256Encoder`, and `getI256Decoder` for signed values in `[-(2^255), 2^255 - 1]`. Both serialize as 32 bytes, honour the `endian` option, and always decode to `BigInt`, matching the existing 64-bit and 128-bit codecs.\n\n`solana_kit_codecs_core` gains tap helpers that observe values or bytes without modifying them. Because any callback may throw, they double as validation guards that need no identity `transformEncoder`:\n\n```dart\nfinal guarded = tapDecoderBytes(getU8Decoder(), (bytes, offset) {\n  if (bytes[offset] > 1) throw StateError('Expected a 0 or a 1');\n});\n\nfinal spanned = tapEncoderBytes(getU8Encoder(), (bytes, pre, post) {\n  print('wrote ${post - pre} bytes at $pre');\n});\n```\n\n`tapEncoder`, `tapDecoder`, and `tapCodec` observe values; `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe bytes and offsets. Each wrapper preserves the size characteristics of the codec it wraps, so `FixedSizeEncoder` stays fixed-size and `VariableSizeEncoder` keeps its `maxSize`.\n\nNote that Dart's `Codec` is not an `Encoder` or a `Decoder`, so the value-level wrappers are typed against the encoder or decoder they observe; use `tapCodec` and `tapCodecBytes` to wrap a codec.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)\n\n#### Add the upstream size-prefix and UTF-8 codec options\n\n_Packages:_ _solana_kit_codecs_data_structures_, _solana_kit_codecs_strings_\n\nPorts the last two surfaces from upstream `@solana/kit` v8.3.0. Both are additive: no existing behavior changes.\n\n`requireSizePrefix` is a named parameter on `getArrayDecoder`, `getArrayCodec`, `getSetDecoder`, `getSetCodec`, `getMapDecoder`, and `getMapCodec`. Upstream defaults it to `false`, where an exhausted byte array decodes as an empty collection so a collection can be appended to an existing layout. This port defaults it to `true` and throws, because a silently empty collection hides truncated input. Pass `requireSizePrefix: false` to opt in:\n\n```dart\nfinal lenient = getArrayCodec(getU8Codec(), requireSizePrefix: false);\nlenient.decode(Uint8List(0)); // []\ngetArrayCodec(getU8Codec()).decode(Uint8List(0)); // throws\n```\n\n`Utf8CodecConfig` is accepted by `getUtf8Encoder`, `getUtf8Decoder`, and `getUtf8Codec`, and carries upstream's three options:\n\n- `fatal` rejects malformed input instead of replacing it. Upstream defaults to `false`, decoding bad bytes as `U+FFFD`; this port defaults to `true` and raises a `FormatException` from decoding and, for lone surrogates, from encoding.\n- `ignoreBOM` preserves a leading byte order mark. Both default to `false`, which strips it, matching Dart's `Utf8Decoder`.\n- `removeNullCharacters` strips `U+0000` from decoded strings. Upstream defaults to `true`; this port defaults to `false` so the decoded value reflects the bytes exactly.\n\nThe two divergent defaults exist so that malformed or null-padded account and instruction data cannot decode silently. To take upstream's behavior explicitly:\n\n```dart\nfinal codec = getUtf8Codec(\n  const Utf8CodecConfig(fatal: false, removeNullCharacters: true),\n);\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [cf4ab87](https://github.com/openbudgetfun/solana_kit/commit/cf4ab873e32bb2ce2f4fe69e73fbc8aa6162894b)\n\n#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n_Packages:_ _solana_kit_codecs_strings_\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)\n\n### Fixes\n\n#### Add scoped mutation testing tooling\n\n_Packages:_ _solana_kit_\n\nAdds a mutation testing setup built on `mutation_test`, so the suite can be checked for tests that assert current behavior rather than the intended contract. Coverage cannot detect that failure mode: a test asserting the wrong contract still executes every line.\n\nFour devenv tasks wrap a new driver at `scripts/run_mutation_testing.dart`:\n\n- `mutation:list` shows the configured scopes and their sizes.\n- `mutation:check` reports how completely each scope's test list covers its transitive dependents.\n- `mutation:changed` runs only the scopes touched since `origin/main`.\n- `mutation:run` runs a scope directly, with `--scope`, `--full`, and `--coverage` options.\n\nScopes live in `config/mutation/scopes.json` and pair source files with the test directories that can detect a change in them, covering the numeric codecs, transaction messages, transaction envelopes, string codecs, codec core, keys, PDAs, hashing, and the untrusted-input parsers.\n\nA default run executes only a scope's listed tests, which costs about two seconds per mutant. Because shared packages have many dependents (`solana_kit_codecs_numbers` has 58, `solana_kit_codecs_core` has 59), a survivor in code other packages exercise can be a false positive; `--full` expands the run to every dependent so no survivor is. `mutation:check` reports the size of that gap per scope.\n\n`config/mutation/rules.xml` replaces the builtin rule set, which is unsuitable for Dart: the builtin `<` rule also matches the first character of `<<` and the builtin argument rules reorder call arguments, so both produce mutations that fail to compile and are then reported as survivors. The rules here use lookarounds to match genuine comparisons, and exclude comments and string literals.\n\nThis is advisory tooling only. It is not wired into CI: equivalent mutants survive legitimately and would make a required check permanently red. See `docs/agents/mutation-testing.md`.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [186ba3d](https://github.com/openbudgetfun/solana_kit/commit/186ba3d9b982b64f6d32156d41f59465fc5770b2)\n\n#### Restore complete version inventories in package READMEs\n\n_Packages:_ _solana_kit_anchor_, _solana_kit_jupiter_, _solana_kit_mpl_core_, _solana_kit_mpl_token_metadata_, _solana_kit_pyth_, _solana_kit_sns_, _solana_kit_squads_\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)\n\n#### Verify both hand-written SHA-256 implementations against an independent hash\n\n_Packages:_ _solana_kit_anchor_, _solana_kit_sns_\n\n`solana_kit_anchor` and `solana_kit_sns` each carry their own pure-Dart SHA-256. Anchor uses it to derive program discriminators and SNS to derive name account addresses, so a defect in either changes which instruction or account a program resolves to — a wrong result rather than an exception. Both were covered only by a handful of fixed NIST vectors, which pin the round function but rarely land on the padding and block boundaries where a length bug hides.\n\nEach package now has a `sha256_differential_test.dart` comparing its implementation against `package:crypto` (the same dependency `solana_kit_addresses` already uses). Coverage includes every input length from 0 to 200, which crosses the 55/56-byte padding transitions and the 64-byte block boundary, lengths from 255 through 10000 for multi-block behavior, randomized inputs, and all-zero and all-`0xff` buffers. Anchor additionally checks `instructionDiscriminator` against the truncated namespaced digest, and SNS checks `getHashedName` against `sha256(\"SPL Name Service\" + name)`.\n\nBoth implementations agree with the reference on every case. No library behavior changes.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)\n\n#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\n_Packages:_ _solana_kit_addresses_, _solana_kit_codecs_strings_, _solana_kit_fixed_points_, _solana_kit_rpc_spec_types_, _solana_kit_transaction_messages_\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)\n\n#### Reject truncated numeric reads with `SolanaError` instead of `RangeError`\n\n_Packages:_ _solana_kit_codecs_numbers_, _solana_kit_transaction_messages_, _solana_kit_transactions_\n\nA decoder reading a fixed-width field out of malformed wire data could raise a raw `RangeError` or `IndexError` out of the SDK instead of the documented `SolanaError`, so a caller catching `SolanaError` saw an escaped exception rather than a rejection it could handle. A truncated account, transaction, or RPC payload reaching the decoders was enough to trigger it; no signature or cluster access was required.\n\nThe guard that upstream `@solana/kit` applies in its number decoder factory was missing from three ports of it. `numberDecoderFactory` and `floatDecoderFactory` read through a `ByteData` view without first checking that the requested width was available, and the six `BigInt` decoders (`u64`, `i64`, `u128`, `i128`, `u256`, `i256`) indexed bytes directly with the same gap. All eight now call `assertByteArrayIsNotEmptyForCodec` and `assertByteArrayHasEnoughBytesForCodec` before reading, raising `codecsCannotDecodeEmptyByteArray` or `codecsInvalidByteLength` as upstream does. A new `bigIntDecoderFactory` carries the guard for the multi-word widths.\n\nTwo further escape sites in `solana_kit_transaction_messages` are fixed. The version 1 instruction payload slice computed `pos + numInstructionDataBytes` and sliced without confirming the buffer held that many bytes; it now asserts the length the way upstream's `fixDecoderSize` wrapper does. The transaction version decoder read `bytes[offset]` unguarded — upstream tolerates this because JavaScript yields `undefined`, which then silently takes the legacy branch and misreports a truncated buffer as an unversioned message, so this port rejects the empty buffer rather than reproducing that fallback.\n\nEncoders are unchanged and still raise `RangeError` when a destination buffer is too small. Upstream writes into a scratch buffer and then `bytes.set`s it, which throws in JavaScript too, so that behavior is deliberate parity rather than a defect.\n\nCallers that caught `RangeError` around a decode should catch `SolanaError`. Callers that already caught `SolanaError` now see malformed input rejected where it previously surfaced as a crash.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)\n\n#### Track @solana/kit v8.3.0\n\n_Packages:_ _solana_kit_errors_\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)\n\n#### Raise the Dart and Flutter baseline\n\n_Packages:_ _solana_kit_test_matchers_\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n#### Add version 1 transaction coverage with oversized payloads\n\n_Packages:_ _solana_kit_transactions_\n\nAdds regression tests for the version 1 transaction wire format introduced by SIMD-0296 and SIMD-0385, which raise the transaction size ceiling from 1232 to 4096 bytes.\n\n`packages/solana_kit_transactions/test/v1_transaction_test.dart` pins the exact bytes against vectors generated by upstream `@solana/kit` 8.3.0, so a regression in the v1 envelope, config mask, or config value encoding fails loudly instead of silently producing transactions the cluster rejects. The vectors live in `test/fixtures/v1_wire_vectors.json`, alongside a README documenting how to regenerate them; the largest asserts a 3216-byte transaction byte-for-byte, which no legacy transaction can represent.\n\nOn-chain coverage lands in `packages/solana_kit_integration_tests/test/integration/v1_transaction_test.dart`. It submits a v1 transaction with a 1600-byte memo — over the legacy ceiling — and asserts the node accepts it, recovers the full memo, reports the inline `transactionConfig` with the requested compute unit and loaded accounts limits, and moves lamports. The same suite asserts the node rejects a payload above 4096 bytes.\n\nA second on-chain suite, `resource_limit_estimation_test.dart`, submits a v1 transaction whose limits were produced by `estimateResourceLimitsFactory` and asserts the node executes it.\n\n`IntegrationTestEnv` gains `sendV1Instructions` and `buildV1WireTransaction` helpers, and `fetchTransaction` now requests `maxSupportedTransactionVersion: 1` so v1 responses can be read back rather than failing with RPC error `-32015`. No public library behavior changes.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "codama-renderers-dart",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/codama-renderers-dart/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "codama-renderers-dart [0.5.6](https://github.com/openbudgetfun/solana_kit/releases/tag/codama-renderers-dart/v0.5.6) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Track Token-2022 js@v0.18.0\n\n`solana_kit_token_2022` follows `solana-program/token-2022` to `js@v0.18.0`, up from `js@v0.16.1`. The pin had been held back because the migration needed handwritten-layer work; that work is now done.\n\nThe generated extension union renames its variants to the renderer's current convention, prefixing each with the union name: `TransferFeeConfig` becomes `ExtensionTransferFeeConfig`, `MetadataPointer` becomes `ExtensionMetadataPointer`, and so on for all twenty-nine variants. The prefix keeps variants readable at the call site and stops them from colliding with same-named generated types. Update pattern matches and constructors:\n\n```dart\nfinal instructions = getPreInitializeInstructionsForMintExtensions(\n  mint: mintAddress,\n  extensions: [\n    ExtensionTransferFeeConfig(\n      transferFeeConfigAuthority: authority,\n      withdrawWithheldAuthority: authority,\n      withheldAmount: BigInt.zero,\n      olderTransferFee: olderFee,\n      newerTransferFee: newerFee,\n    ),\n  ],\n);\n```\n\nMint and token accounts now decode their TLV extension region the way the program reads it. The previous codec decoded the region as a `remainder` array, which had to consume every trailing byte as an entry, so an account allocated with unused space either threw or reported the padding as a spurious `Uninitialized` extension. The new codec walks entries until it meets an `Uninitialized` (type 0) header or fewer than two bytes remain, and ignores whatever follows, matching the program and `@solana/spl-token`:\n\n```dart\nfinal account = getMintDecoder().decode(bytesWithTwoBytesOfPadding);\n// extensions now holds only the real entries, never the padding.\n```\n\n`solana_kit_address_constants`-style address handling is unchanged; `getMintSize` and `getTokenSize` keep their existing signatures and results, and now share the same encoder the accounts use, so a size computed from a list of extensions always matches what the encoder writes.\n\n`codama-renderers-dart` gains `linkOverrides`, which lets an IDL link be backed by a hand-written Dart codec while the wrappers around it (`Option`, `HiddenPrefix`, …) still come from the IDL. That is what the Token-2022 extension region uses, and it is the extension point for any future type Codama cannot express. The renderer also stops requiring an account to match its size discriminator exactly: a size discriminator describes an account's fixed prefix, so accounts with variable trailing fields are legitimately longer. Instruction sizes are still matched exactly, since there the size identifies the whole payload.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Update the renderer toolchain to TypeScript 7 and Vitest 5\n\nThe build and test toolchain moves to TypeScript 7.0.2, Vitest 5.0.0, `@vitest/coverage-v8` 5.0.0, and `@types/node` 26.5.1. Generated Dart output is unchanged; this is a tooling-only release.\n\nTypeScript 6 and later no longer discover `@types` packages implicitly under `moduleResolution: \"bundler\"`, and they require an explicit common source directory when emitting declarations. Both projects therefore declare the Node types and the declaration root directory:\n\n```jsonc\n// tsconfig.json\n{ \"compilerOptions\": { \"types\": [\"node\"] } }\n// tsconfig.declarations.json\n{ \"compilerOptions\": { \"rootDir\": \"./src\" } }\n```\n\nThe Vitest config also sets explicit `testTimeout` and `hookTimeout` budgets. Most tests in this package shell out to `dart format`, `dart analyze`, or `dart test`, so the 5s default was already marginal for the heaviest cases.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [0d78dde](https://github.com/openbudgetfun/solana_kit/commit/0d78dde5ec3f5c1971b8b3b7f97437b12f9d8e02)",
              "- **Add the MIT LICENSE file to the renderer package.** `packages/codama-renderers-dart` declared `\"license\": \"MIT\"` in its `package.json` but never shipped the license text itself. The file now matches the MIT LICENSE used by every other package in the workspace, satisfying the repo-wide package rule and making the license discoverable from a plain clone of the npm package.\n  _Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)",
              "#### Track the memo v4 program and add memo extraction helpers\n\nThe workspace now tracks `solana-program/memo` at `js@v0.14.1` (previously `js@v0.13.1`). The `mpl-token-metadata` reference pin also moves to `353d01be4af3`; its IDL is byte-identical, so that package is unaffected.\n\n`solana_kit_address_constants` moves `memoProgramAddress` to the v4 memo program (`Memo4c2pN8afCj432Lb7RMVKi9PbQnnW7ewFFaV3oAH`), matching the upstream IDL `publicKey` as of `js@v0.14.0`. The previous v3 address stays available as the new `memoLegacyProgramAddressV3` constant, and `memoLegacyProgramAddress` (v1) is unchanged. Code that builds new memo instructions picks up the v4 program automatically; code that must keep targeting v3 names the legacy constant explicitly.\n\n`solana_kit_memo` ports the upstream extraction helpers from `js@v0.14.1`:\n\n- `getMemosFromInstructions` scans a list of instructions, matches every deployed Memo program address, and returns the UTF-8 decoded memo text with the raw bytes, source program address, and instruction index.\n- `ExtractedMemo` carries one extracted memo.\n- `supportedMemoProgramAddresses` lists every deployed Memo program address ordered from oldest (v1) to newest (v4).\n\nThe generated layer is regenerated against `js@v0.14.1` with the current renderer: the program page is byte-identical because the program address flows through the well-known constants, and the AddMemo data decoder now validates byte length strictly, throwing `SolanaError` with `codecsInvalidByteLength` on trailing bytes. `solana_kit_errors` moves from a dev dependency to a dependency because the generated decoder references it.\n\n`codama-renderers-dart` maps the v4 address to `memoProgramAddress` and the v3 address to `memoLegacyProgramAddressV3` in its well-known address registry, so regenerated clients re-export the canonical constants instead of hardcoding address strings.\n\nBuild new memo instructions against `memoProgramAddress` (v4). Where a memo must be executed by a program the runtime provides, check what is deployed: SurfPool, used by this repository's on-chain integration tests, ships the v1 and v3 programs as executable bytecode but resolves v4 to a placeholder, so those tests invoke `memoLegacyProgramAddressV3`. All three programs share the same instruction format, so only the program the transaction targets differs.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)",
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## codama-renderers-dart [0.5.6](https://github.com/openbudgetfun/solana_kit/releases/tag/codama-renderers-dart/v0.5.6) (2026-09-21)\n\n### Features\n\n#### Track Token-2022 js@v0.18.0\n\n`solana_kit_token_2022` follows `solana-program/token-2022` to `js@v0.18.0`, up from `js@v0.16.1`. The pin had been held back because the migration needed handwritten-layer work; that work is now done.\n\nThe generated extension union renames its variants to the renderer's current convention, prefixing each with the union name: `TransferFeeConfig` becomes `ExtensionTransferFeeConfig`, `MetadataPointer` becomes `ExtensionMetadataPointer`, and so on for all twenty-nine variants. The prefix keeps variants readable at the call site and stops them from colliding with same-named generated types. Update pattern matches and constructors:\n\n```dart\nfinal instructions = getPreInitializeInstructionsForMintExtensions(\n  mint: mintAddress,\n  extensions: [\n    ExtensionTransferFeeConfig(\n      transferFeeConfigAuthority: authority,\n      withdrawWithheldAuthority: authority,\n      withheldAmount: BigInt.zero,\n      olderTransferFee: olderFee,\n      newerTransferFee: newerFee,\n    ),\n  ],\n);\n```\n\nMint and token accounts now decode their TLV extension region the way the program reads it. The previous codec decoded the region as a `remainder` array, which had to consume every trailing byte as an entry, so an account allocated with unused space either threw or reported the padding as a spurious `Uninitialized` extension. The new codec walks entries until it meets an `Uninitialized` (type 0) header or fewer than two bytes remain, and ignores whatever follows, matching the program and `@solana/spl-token`:\n\n```dart\nfinal account = getMintDecoder().decode(bytesWithTwoBytesOfPadding);\n// extensions now holds only the real entries, never the padding.\n```\n\n`solana_kit_address_constants`-style address handling is unchanged; `getMintSize` and `getTokenSize` keep their existing signatures and results, and now share the same encoder the accounts use, so a size computed from a list of extensions always matches what the encoder writes.\n\n`codama-renderers-dart` gains `linkOverrides`, which lets an IDL link be backed by a hand-written Dart codec while the wrappers around it (`Option`, `HiddenPrefix`, …) still come from the IDL. That is what the Token-2022 extension region uses, and it is the extension point for any future type Codama cannot express. The renderer also stops requiring an account to match its size discriminator exactly: a size discriminator describes an account's fixed prefix, so accounts with variable trailing fields are legitimately longer. Instruction sizes are still matched exactly, since there the size identifies the whole payload.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)\n\n### Fixes\n\n#### Update the renderer toolchain to TypeScript 7 and Vitest 5\n\nThe build and test toolchain moves to TypeScript 7.0.2, Vitest 5.0.0, `@vitest/coverage-v8` 5.0.0, and `@types/node` 26.5.1. Generated Dart output is unchanged; this is a tooling-only release.\n\nTypeScript 6 and later no longer discover `@types` packages implicitly under `moduleResolution: \"bundler\"`, and they require an explicit common source directory when emitting declarations. Both projects therefore declare the Node types and the declaration root directory:\n\n```jsonc\n// tsconfig.json\n{ \"compilerOptions\": { \"types\": [\"node\"] } }\n// tsconfig.declarations.json\n{ \"compilerOptions\": { \"rootDir\": \"./src\" } }\n```\n\nThe Vitest config also sets explicit `testTimeout` and `hookTimeout` budgets. Most tests in this package shell out to `dart format`, `dart analyze`, or `dart test`, so the 5s default was already marginal for the heaviest cases.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [0d78dde](https://github.com/openbudgetfun/solana_kit/commit/0d78dde5ec3f5c1971b8b3b7f97437b12f9d8e02)\n\n- **Add the MIT LICENSE file to the renderer package.** `packages/codama-renderers-dart` declared `\"license\": \"MIT\"` in its `package.json` but never shipped the license text itself. The file now matches the MIT LICENSE used by every other package in the workspace, satisfying the repo-wide package rule and making the license discoverable from a plain clone of the npm package.\n  _Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)\n#### Track the memo v4 program and add memo extraction helpers\n\nThe workspace now tracks `solana-program/memo` at `js@v0.14.1` (previously `js@v0.13.1`). The `mpl-token-metadata` reference pin also moves to `353d01be4af3`; its IDL is byte-identical, so that package is unaffected.\n\n`solana_kit_address_constants` moves `memoProgramAddress` to the v4 memo program (`Memo4c2pN8afCj432Lb7RMVKi9PbQnnW7ewFFaV3oAH`), matching the upstream IDL `publicKey` as of `js@v0.14.0`. The previous v3 address stays available as the new `memoLegacyProgramAddressV3` constant, and `memoLegacyProgramAddress` (v1) is unchanged. Code that builds new memo instructions picks up the v4 program automatically; code that must keep targeting v3 names the legacy constant explicitly.\n\n`solana_kit_memo` ports the upstream extraction helpers from `js@v0.14.1`:\n\n- `getMemosFromInstructions` scans a list of instructions, matches every deployed Memo program address, and returns the UTF-8 decoded memo text with the raw bytes, source program address, and instruction index.\n- `ExtractedMemo` carries one extracted memo.\n- `supportedMemoProgramAddresses` lists every deployed Memo program address ordered from oldest (v1) to newest (v4).\n\nThe generated layer is regenerated against `js@v0.14.1` with the current renderer: the program page is byte-identical because the program address flows through the well-known constants, and the AddMemo data decoder now validates byte length strictly, throwing `SolanaError` with `codecsInvalidByteLength` on trailing bytes. `solana_kit_errors` moves from a dev dependency to a dependency because the generated decoder references it.\n\n`codama-renderers-dart` maps the v4 address to `memoProgramAddress` and the v3 address to `memoLegacyProgramAddressV3` in its well-known address registry, so regenerated clients re-export the canonical constants instead of hardcoding address strings.\n\nBuild new memo instructions against `memoProgramAddress` (v4). Where a memo must be executed by a program the runtime provides, check what is deployed: SurfPool, used by this repository's on-chain integration tests, ships the v1 and v3 programs as executable bytecode but resolves v4 to a placeholder, so those tests invoke `memoLegacyProgramAddressV3`. All three programs share the same instruction format, so only the program the transaction targets differs.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)",
              "#### Track @solana/kit v8.3.0\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Add scoped mutation testing tooling\n\nAdds a mutation testing setup built on `mutation_test`, so the suite can be checked for tests that assert current behavior rather than the intended contract. Coverage cannot detect that failure mode: a test asserting the wrong contract still executes every line.\n\nFour devenv tasks wrap a new driver at `scripts/run_mutation_testing.dart`:\n\n- `mutation:list` shows the configured scopes and their sizes.\n- `mutation:check` reports how completely each scope's test list covers its transitive dependents.\n- `mutation:changed` runs only the scopes touched since `origin/main`.\n- `mutation:run` runs a scope directly, with `--scope`, `--full`, and `--coverage` options.\n\nScopes live in `config/mutation/scopes.json` and pair source files with the test directories that can detect a change in them, covering the numeric codecs, transaction messages, transaction envelopes, string codecs, codec core, keys, PDAs, hashing, and the untrusted-input parsers.\n\nA default run executes only a scope's listed tests, which costs about two seconds per mutant. Because shared packages have many dependents (`solana_kit_codecs_numbers` has 58, `solana_kit_codecs_core` has 59), a survivor in code other packages exercise can be a false positive; `--full` expands the run to every dependent so no survivor is. `mutation:check` reports the size of that gap per scope.\n\n`config/mutation/rules.xml` replaces the builtin rule set, which is unsuitable for Dart: the builtin `<` rule also matches the first character of `<<` and the builtin argument rules reorder call arguments, so both produce mutations that fail to compile and are then reported as survivors. The rules here use lookarounds to match genuine comparisons, and exclude comments and string literals.\n\nThis is advisory tooling only. It is not wired into CI: equivalent mutants survive legitimately and would make a required check permanently red. See `docs/agents/mutation-testing.md`.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [186ba3d](https://github.com/openbudgetfun/solana_kit/commit/186ba3d9b982b64f6d32156d41f59465fc5770b2)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n#### Track @solana/kit v8.3.0\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)\n\n### Fixes\n\n#### Add scoped mutation testing tooling\n\nAdds a mutation testing setup built on `mutation_test`, so the suite can be checked for tests that assert current behavior rather than the intended contract. Coverage cannot detect that failure mode: a test asserting the wrong contract still executes every line.\n\nFour devenv tasks wrap a new driver at `scripts/run_mutation_testing.dart`:\n\n- `mutation:list` shows the configured scopes and their sizes.\n- `mutation:check` reports how completely each scope's test list covers its transitive dependents.\n- `mutation:changed` runs only the scopes touched since `origin/main`.\n- `mutation:run` runs a scope directly, with `--scope`, `--full`, and `--coverage` options.\n\nScopes live in `config/mutation/scopes.json` and pair source files with the test directories that can detect a change in them, covering the numeric codecs, transaction messages, transaction envelopes, string codecs, codec core, keys, PDAs, hashing, and the untrusted-input parsers.\n\nA default run executes only a scope's listed tests, which costs about two seconds per mutant. Because shared packages have many dependents (`solana_kit_codecs_numbers` has 58, `solana_kit_codecs_core` has 59), a survivor in code other packages exercise can be a false positive; `--full` expands the run to every dependent so no survivor is. `mutation:check` reports the size of that gap per scope.\n\n`config/mutation/rules.xml` replaces the builtin rule set, which is unsuitable for Dart: the builtin `<` rule also matches the first character of `<<` and the builtin argument rules reorder call arguments, so both produce mutations that fail to compile and are then reported as survivors. The rules here use lookarounds to match genuine comparisons, and exclude comments and string literals.\n\nThis is advisory tooling only. It is not wired into CI: equivalent mutants survive legitimately and would make a required check permanently red. See `docs/agents/mutation-testing.md`.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [186ba3d](https://github.com/openbudgetfun/solana_kit/commit/186ba3d9b982b64f6d32156d41f59465fc5770b2)"
    },
    {
      "owner_id": "solana_kit_accounts",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_accounts/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_address",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_address/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_address_constants",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_address_constants/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Track the memo v4 program and add memo extraction helpers\n\nThe workspace now tracks `solana-program/memo` at `js@v0.14.1` (previously `js@v0.13.1`). The `mpl-token-metadata` reference pin also moves to `353d01be4af3`; its IDL is byte-identical, so that package is unaffected.\n\n`solana_kit_address_constants` moves `memoProgramAddress` to the v4 memo program (`Memo4c2pN8afCj432Lb7RMVKi9PbQnnW7ewFFaV3oAH`), matching the upstream IDL `publicKey` as of `js@v0.14.0`. The previous v3 address stays available as the new `memoLegacyProgramAddressV3` constant, and `memoLegacyProgramAddress` (v1) is unchanged. Code that builds new memo instructions picks up the v4 program automatically; code that must keep targeting v3 names the legacy constant explicitly.\n\n`solana_kit_memo` ports the upstream extraction helpers from `js@v0.14.1`:\n\n- `getMemosFromInstructions` scans a list of instructions, matches every deployed Memo program address, and returns the UTF-8 decoded memo text with the raw bytes, source program address, and instruction index.\n- `ExtractedMemo` carries one extracted memo.\n- `supportedMemoProgramAddresses` lists every deployed Memo program address ordered from oldest (v1) to newest (v4).\n\nThe generated layer is regenerated against `js@v0.14.1` with the current renderer: the program page is byte-identical because the program address flows through the well-known constants, and the AddMemo data decoder now validates byte length strictly, throwing `SolanaError` with `codecsInvalidByteLength` on trailing bytes. `solana_kit_errors` moves from a dev dependency to a dependency because the generated decoder references it.\n\n`codama-renderers-dart` maps the v4 address to `memoProgramAddress` and the v3 address to `memoLegacyProgramAddressV3` in its well-known address registry, so regenerated clients re-export the canonical constants instead of hardcoding address strings.\n\nBuild new memo instructions against `memoProgramAddress` (v4). Where a memo must be executed by a program the runtime provides, check what is deployed: SurfPool, used by this repository's on-chain integration tests, ships the v1 and v3 programs as executable bytecode but resolves v4 to a placeholder, so those tests invoke `memoLegacyProgramAddressV3`. All three programs share the same instruction format, so only the program the transaction targets differs.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Track the memo v4 program and add memo extraction helpers\n\nThe workspace now tracks `solana-program/memo` at `js@v0.14.1` (previously `js@v0.13.1`). The `mpl-token-metadata` reference pin also moves to `353d01be4af3`; its IDL is byte-identical, so that package is unaffected.\n\n`solana_kit_address_constants` moves `memoProgramAddress` to the v4 memo program (`Memo4c2pN8afCj432Lb7RMVKi9PbQnnW7ewFFaV3oAH`), matching the upstream IDL `publicKey` as of `js@v0.14.0`. The previous v3 address stays available as the new `memoLegacyProgramAddressV3` constant, and `memoLegacyProgramAddress` (v1) is unchanged. Code that builds new memo instructions picks up the v4 program automatically; code that must keep targeting v3 names the legacy constant explicitly.\n\n`solana_kit_memo` ports the upstream extraction helpers from `js@v0.14.1`:\n\n- `getMemosFromInstructions` scans a list of instructions, matches every deployed Memo program address, and returns the UTF-8 decoded memo text with the raw bytes, source program address, and instruction index.\n- `ExtractedMemo` carries one extracted memo.\n- `supportedMemoProgramAddresses` lists every deployed Memo program address ordered from oldest (v1) to newest (v4).\n\nThe generated layer is regenerated against `js@v0.14.1` with the current renderer: the program page is byte-identical because the program address flows through the well-known constants, and the AddMemo data decoder now validates byte length strictly, throwing `SolanaError` with `codecsInvalidByteLength` on trailing bytes. `solana_kit_errors` moves from a dev dependency to a dependency because the generated decoder references it.\n\n`codama-renderers-dart` maps the v4 address to `memoProgramAddress` and the v3 address to `memoLegacyProgramAddressV3` in its well-known address registry, so regenerated clients re-export the canonical constants instead of hardcoding address strings.\n\nBuild new memo instructions against `memoProgramAddress` (v4). Where a memo must be executed by a program the runtime provides, check what is deployed: SurfPool, used by this repository's on-chain integration tests, ships the v1 and v3 programs as executable bytecode but resolves v4 to a placeholder, so those tests invoke `memoLegacyProgramAddressV3`. All three programs share the same instruction format, so only the program the transaction targets differs.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)"
    },
    {
      "owner_id": "solana_kit_address_lookup_table",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_address_lookup_table/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_address_lookup_table [0.5.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_address_lookup_table/v0.5.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_address_lookup_table [0.5.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_address_lookup_table/v0.5.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_addresses",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_addresses/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)"
    },
    {
      "owner_id": "solana_kit_anchor",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_anchor/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)",
              "#### Verify both hand-written SHA-256 implementations against an independent hash\n\n`solana_kit_anchor` and `solana_kit_sns` each carry their own pure-Dart SHA-256. Anchor uses it to derive program discriminators and SNS to derive name account addresses, so a defect in either changes which instruction or account a program resolves to — a wrong result rather than an exception. Both were covered only by a handful of fixed NIST vectors, which pin the round function but rarely land on the padding and block boundaries where a length bug hides.\n\nEach package now has a `sha256_differential_test.dart` comparing its implementation against `package:crypto` (the same dependency `solana_kit_addresses` already uses). Coverage includes every input length from 0 to 200, which crosses the 55/56-byte padding transitions and the 64-byte block boundary, lengths from 255 through 10000 for multi-block behavior, randomized inputs, and all-zero and all-`0xff` buffers. Anchor additionally checks `instructionDiscriminator` against the truncated namespaced digest, and SNS checks `getHashedName` against `sha256(\"SPL Name Service\" + name)`.\n\nBoth implementations agree with the reference on every case. No library behavior changes.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)\n\n#### Verify both hand-written SHA-256 implementations against an independent hash\n\n`solana_kit_anchor` and `solana_kit_sns` each carry their own pure-Dart SHA-256. Anchor uses it to derive program discriminators and SNS to derive name account addresses, so a defect in either changes which instruction or account a program resolves to — a wrong result rather than an exception. Both were covered only by a handful of fixed NIST vectors, which pin the round function but rarely land on the padding and block boundaries where a length bug hides.\n\nEach package now has a `sha256_differential_test.dart` comparing its implementation against `package:crypto` (the same dependency `solana_kit_addresses` already uses). Coverage includes every input length from 0 to 200, which crosses the 55/56-byte padding transitions and the 64-byte block boundary, lengths from 255 through 10000 for multi-block behavior, randomized inputs, and all-zero and all-`0xff` buffers. Anchor additionally checks `instructionDiscriminator` against the truncated namespaced digest, and SNS checks `getHashedName` against `sha256(\"SPL Name Service\" + name)`.\n\nBoth implementations agree with the reference on every case. No library behavior changes.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)"
    },
    {
      "owner_id": "solana_kit_associated_token_account",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_associated_token_account/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_associated_token_account [0.8.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_associated_token_account/v0.8.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_associated_token_account [0.8.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_associated_token_account/v0.8.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_attestation_service",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_attestation_service/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_attestation_service [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_attestation_service/v0.2.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)",
              "#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_attestation_service [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_attestation_service/v0.2.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)\n\n#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
    },
    {
      "owner_id": "solana_kit_codecs",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_codecs/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_codecs_core",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_codecs_core/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Add 256-bit number codecs and tap codec helpers\n\nPorts the two additive codec surfaces from upstream `@solana/kit` v8.3.0.\n\n`solana_kit_codecs_numbers` gains 256-bit integer codecs: `getU256Codec`, `getU256Encoder`, and `getU256Decoder` for unsigned values in `[0, 2^256 - 1]`, plus `getI256Codec`, `getI256Encoder`, and `getI256Decoder` for signed values in `[-(2^255), 2^255 - 1]`. Both serialize as 32 bytes, honour the `endian` option, and always decode to `BigInt`, matching the existing 64-bit and 128-bit codecs.\n\n`solana_kit_codecs_core` gains tap helpers that observe values or bytes without modifying them. Because any callback may throw, they double as validation guards that need no identity `transformEncoder`:\n\n```dart\nfinal guarded = tapDecoderBytes(getU8Decoder(), (bytes, offset) {\n  if (bytes[offset] > 1) throw StateError('Expected a 0 or a 1');\n});\n\nfinal spanned = tapEncoderBytes(getU8Encoder(), (bytes, pre, post) {\n  print('wrote ${post - pre} bytes at $pre');\n});\n```\n\n`tapEncoder`, `tapDecoder`, and `tapCodec` observe values; `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe bytes and offsets. Each wrapper preserves the size characteristics of the codec it wraps, so `FixedSizeEncoder` stays fixed-size and `VariableSizeEncoder` keeps its `maxSize`.\n\nNote that Dart's `Codec` is not an `Encoder` or a `Decoder`, so the value-level wrappers are typed against the encoder or decoder they observe; use `tapCodec` and `tapCodecBytes` to wrap a codec.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)",
              "#### Track @solana/kit v8.3.0\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Add 256-bit number codecs and tap codec helpers\n\nPorts the two additive codec surfaces from upstream `@solana/kit` v8.3.0.\n\n`solana_kit_codecs_numbers` gains 256-bit integer codecs: `getU256Codec`, `getU256Encoder`, and `getU256Decoder` for unsigned values in `[0, 2^256 - 1]`, plus `getI256Codec`, `getI256Encoder`, and `getI256Decoder` for signed values in `[-(2^255), 2^255 - 1]`. Both serialize as 32 bytes, honour the `endian` option, and always decode to `BigInt`, matching the existing 64-bit and 128-bit codecs.\n\n`solana_kit_codecs_core` gains tap helpers that observe values or bytes without modifying them. Because any callback may throw, they double as validation guards that need no identity `transformEncoder`:\n\n```dart\nfinal guarded = tapDecoderBytes(getU8Decoder(), (bytes, offset) {\n  if (bytes[offset] > 1) throw StateError('Expected a 0 or a 1');\n});\n\nfinal spanned = tapEncoderBytes(getU8Encoder(), (bytes, pre, post) {\n  print('wrote ${post - pre} bytes at $pre');\n});\n```\n\n`tapEncoder`, `tapDecoder`, and `tapCodec` observe values; `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe bytes and offsets. Each wrapper preserves the size characteristics of the codec it wraps, so `FixedSizeEncoder` stays fixed-size and `VariableSizeEncoder` keeps its `maxSize`.\n\nNote that Dart's `Codec` is not an `Encoder` or a `Decoder`, so the value-level wrappers are typed against the encoder or decoder they observe; use `tapCodec` and `tapCodecBytes` to wrap a codec.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)\n\n#### Track @solana/kit v8.3.0\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)"
    },
    {
      "owner_id": "solana_kit_codecs_data_structures",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_codecs_data_structures/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Add the upstream size-prefix and UTF-8 codec options\n\nPorts the last two surfaces from upstream `@solana/kit` v8.3.0. Both are additive: no existing behavior changes.\n\n`requireSizePrefix` is a named parameter on `getArrayDecoder`, `getArrayCodec`, `getSetDecoder`, `getSetCodec`, `getMapDecoder`, and `getMapCodec`. Upstream defaults it to `false`, where an exhausted byte array decodes as an empty collection so a collection can be appended to an existing layout. This port defaults it to `true` and throws, because a silently empty collection hides truncated input. Pass `requireSizePrefix: false` to opt in:\n\n```dart\nfinal lenient = getArrayCodec(getU8Codec(), requireSizePrefix: false);\nlenient.decode(Uint8List(0)); // []\ngetArrayCodec(getU8Codec()).decode(Uint8List(0)); // throws\n```\n\n`Utf8CodecConfig` is accepted by `getUtf8Encoder`, `getUtf8Decoder`, and `getUtf8Codec`, and carries upstream's three options:\n\n- `fatal` rejects malformed input instead of replacing it. Upstream defaults to `false`, decoding bad bytes as `U+FFFD`; this port defaults to `true` and raises a `FormatException` from decoding and, for lone surrogates, from encoding.\n- `ignoreBOM` preserves a leading byte order mark. Both default to `false`, which strips it, matching Dart's `Utf8Decoder`.\n- `removeNullCharacters` strips `U+0000` from decoded strings. Upstream defaults to `true`; this port defaults to `false` so the decoded value reflects the bytes exactly.\n\nThe two divergent defaults exist so that malformed or null-padded account and instruction data cannot decode silently. To take upstream's behavior explicitly:\n\n```dart\nfinal codec = getUtf8Codec(\n  const Utf8CodecConfig(fatal: false, removeNullCharacters: true),\n);\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [cf4ab87](https://github.com/openbudgetfun/solana_kit/commit/cf4ab873e32bb2ce2f4fe69e73fbc8aa6162894b)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Add the upstream size-prefix and UTF-8 codec options\n\nPorts the last two surfaces from upstream `@solana/kit` v8.3.0. Both are additive: no existing behavior changes.\n\n`requireSizePrefix` is a named parameter on `getArrayDecoder`, `getArrayCodec`, `getSetDecoder`, `getSetCodec`, `getMapDecoder`, and `getMapCodec`. Upstream defaults it to `false`, where an exhausted byte array decodes as an empty collection so a collection can be appended to an existing layout. This port defaults it to `true` and throws, because a silently empty collection hides truncated input. Pass `requireSizePrefix: false` to opt in:\n\n```dart\nfinal lenient = getArrayCodec(getU8Codec(), requireSizePrefix: false);\nlenient.decode(Uint8List(0)); // []\ngetArrayCodec(getU8Codec()).decode(Uint8List(0)); // throws\n```\n\n`Utf8CodecConfig` is accepted by `getUtf8Encoder`, `getUtf8Decoder`, and `getUtf8Codec`, and carries upstream's three options:\n\n- `fatal` rejects malformed input instead of replacing it. Upstream defaults to `false`, decoding bad bytes as `U+FFFD`; this port defaults to `true` and raises a `FormatException` from decoding and, for lone surrogates, from encoding.\n- `ignoreBOM` preserves a leading byte order mark. Both default to `false`, which strips it, matching Dart's `Utf8Decoder`.\n- `removeNullCharacters` strips `U+0000` from decoded strings. Upstream defaults to `true`; this port defaults to `false` so the decoded value reflects the bytes exactly.\n\nThe two divergent defaults exist so that malformed or null-padded account and instruction data cannot decode silently. To take upstream's behavior explicitly:\n\n```dart\nfinal codec = getUtf8Codec(\n  const Utf8CodecConfig(fatal: false, removeNullCharacters: true),\n);\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [cf4ab87](https://github.com/openbudgetfun/solana_kit/commit/cf4ab873e32bb2ce2f4fe69e73fbc8aa6162894b)"
    },
    {
      "owner_id": "solana_kit_codecs_numbers",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_codecs_numbers/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Add 256-bit number codecs and tap codec helpers\n\nPorts the two additive codec surfaces from upstream `@solana/kit` v8.3.0.\n\n`solana_kit_codecs_numbers` gains 256-bit integer codecs: `getU256Codec`, `getU256Encoder`, and `getU256Decoder` for unsigned values in `[0, 2^256 - 1]`, plus `getI256Codec`, `getI256Encoder`, and `getI256Decoder` for signed values in `[-(2^255), 2^255 - 1]`. Both serialize as 32 bytes, honour the `endian` option, and always decode to `BigInt`, matching the existing 64-bit and 128-bit codecs.\n\n`solana_kit_codecs_core` gains tap helpers that observe values or bytes without modifying them. Because any callback may throw, they double as validation guards that need no identity `transformEncoder`:\n\n```dart\nfinal guarded = tapDecoderBytes(getU8Decoder(), (bytes, offset) {\n  if (bytes[offset] > 1) throw StateError('Expected a 0 or a 1');\n});\n\nfinal spanned = tapEncoderBytes(getU8Encoder(), (bytes, pre, post) {\n  print('wrote ${post - pre} bytes at $pre');\n});\n```\n\n`tapEncoder`, `tapDecoder`, and `tapCodec` observe values; `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe bytes and offsets. Each wrapper preserves the size characteristics of the codec it wraps, so `FixedSizeEncoder` stays fixed-size and `VariableSizeEncoder` keeps its `maxSize`.\n\nNote that Dart's `Codec` is not an `Encoder` or a `Decoder`, so the value-level wrappers are typed against the encoder or decoder they observe; use `tapCodec` and `tapCodecBytes` to wrap a codec.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)",
              "#### Track @solana/kit v8.3.0\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Reject truncated numeric reads with `SolanaError` instead of `RangeError`\n\nA decoder reading a fixed-width field out of malformed wire data could raise a raw `RangeError` or `IndexError` out of the SDK instead of the documented `SolanaError`, so a caller catching `SolanaError` saw an escaped exception rather than a rejection it could handle. A truncated account, transaction, or RPC payload reaching the decoders was enough to trigger it; no signature or cluster access was required.\n\nThe guard that upstream `@solana/kit` applies in its number decoder factory was missing from three ports of it. `numberDecoderFactory` and `floatDecoderFactory` read through a `ByteData` view without first checking that the requested width was available, and the six `BigInt` decoders (`u64`, `i64`, `u128`, `i128`, `u256`, `i256`) indexed bytes directly with the same gap. All eight now call `assertByteArrayIsNotEmptyForCodec` and `assertByteArrayHasEnoughBytesForCodec` before reading, raising `codecsCannotDecodeEmptyByteArray` or `codecsInvalidByteLength` as upstream does. A new `bigIntDecoderFactory` carries the guard for the multi-word widths.\n\nTwo further escape sites in `solana_kit_transaction_messages` are fixed. The version 1 instruction payload slice computed `pos + numInstructionDataBytes` and sliced without confirming the buffer held that many bytes; it now asserts the length the way upstream's `fixDecoderSize` wrapper does. The transaction version decoder read `bytes[offset]` unguarded — upstream tolerates this because JavaScript yields `undefined`, which then silently takes the legacy branch and misreports a truncated buffer as an unversioned message, so this port rejects the empty buffer rather than reproducing that fallback.\n\nEncoders are unchanged and still raise `RangeError` when a destination buffer is too small. Upstream writes into a scratch buffer and then `bytes.set`s it, which throws in JavaScript too, so that behavior is deliberate parity rather than a defect.\n\nCallers that caught `RangeError` around a decode should catch `SolanaError`. Callers that already caught `SolanaError` now see malformed input rejected where it previously surfaced as a crash.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Add 256-bit number codecs and tap codec helpers\n\nPorts the two additive codec surfaces from upstream `@solana/kit` v8.3.0.\n\n`solana_kit_codecs_numbers` gains 256-bit integer codecs: `getU256Codec`, `getU256Encoder`, and `getU256Decoder` for unsigned values in `[0, 2^256 - 1]`, plus `getI256Codec`, `getI256Encoder`, and `getI256Decoder` for signed values in `[-(2^255), 2^255 - 1]`. Both serialize as 32 bytes, honour the `endian` option, and always decode to `BigInt`, matching the existing 64-bit and 128-bit codecs.\n\n`solana_kit_codecs_core` gains tap helpers that observe values or bytes without modifying them. Because any callback may throw, they double as validation guards that need no identity `transformEncoder`:\n\n```dart\nfinal guarded = tapDecoderBytes(getU8Decoder(), (bytes, offset) {\n  if (bytes[offset] > 1) throw StateError('Expected a 0 or a 1');\n});\n\nfinal spanned = tapEncoderBytes(getU8Encoder(), (bytes, pre, post) {\n  print('wrote ${post - pre} bytes at $pre');\n});\n```\n\n`tapEncoder`, `tapDecoder`, and `tapCodec` observe values; `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe bytes and offsets. Each wrapper preserves the size characteristics of the codec it wraps, so `FixedSizeEncoder` stays fixed-size and `VariableSizeEncoder` keeps its `maxSize`.\n\nNote that Dart's `Codec` is not an `Encoder` or a `Decoder`, so the value-level wrappers are typed against the encoder or decoder they observe; use `tapCodec` and `tapCodecBytes` to wrap a codec.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)\n\n#### Track @solana/kit v8.3.0\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)\n\n### Fixes\n\n#### Reject truncated numeric reads with `SolanaError` instead of `RangeError`\n\nA decoder reading a fixed-width field out of malformed wire data could raise a raw `RangeError` or `IndexError` out of the SDK instead of the documented `SolanaError`, so a caller catching `SolanaError` saw an escaped exception rather than a rejection it could handle. A truncated account, transaction, or RPC payload reaching the decoders was enough to trigger it; no signature or cluster access was required.\n\nThe guard that upstream `@solana/kit` applies in its number decoder factory was missing from three ports of it. `numberDecoderFactory` and `floatDecoderFactory` read through a `ByteData` view without first checking that the requested width was available, and the six `BigInt` decoders (`u64`, `i64`, `u128`, `i128`, `u256`, `i256`) indexed bytes directly with the same gap. All eight now call `assertByteArrayIsNotEmptyForCodec` and `assertByteArrayHasEnoughBytesForCodec` before reading, raising `codecsCannotDecodeEmptyByteArray` or `codecsInvalidByteLength` as upstream does. A new `bigIntDecoderFactory` carries the guard for the multi-word widths.\n\nTwo further escape sites in `solana_kit_transaction_messages` are fixed. The version 1 instruction payload slice computed `pos + numInstructionDataBytes` and sliced without confirming the buffer held that many bytes; it now asserts the length the way upstream's `fixDecoderSize` wrapper does. The transaction version decoder read `bytes[offset]` unguarded — upstream tolerates this because JavaScript yields `undefined`, which then silently takes the legacy branch and misreports a truncated buffer as an unversioned message, so this port rejects the empty buffer rather than reproducing that fallback.\n\nEncoders are unchanged and still raise `RangeError` when a destination buffer is too small. Upstream writes into a scratch buffer and then `bytes.set`s it, which throws in JavaScript too, so that behavior is deliberate parity rather than a defect.\n\nCallers that caught `RangeError` around a decode should catch `SolanaError`. Callers that already caught `SolanaError` now see malformed input rejected where it previously surfaced as a crash.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)"
    },
    {
      "owner_id": "solana_kit_codecs_strings",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_codecs_strings/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Add the upstream size-prefix and UTF-8 codec options\n\nPorts the last two surfaces from upstream `@solana/kit` v8.3.0. Both are additive: no existing behavior changes.\n\n`requireSizePrefix` is a named parameter on `getArrayDecoder`, `getArrayCodec`, `getSetDecoder`, `getSetCodec`, `getMapDecoder`, and `getMapCodec`. Upstream defaults it to `false`, where an exhausted byte array decodes as an empty collection so a collection can be appended to an existing layout. This port defaults it to `true` and throws, because a silently empty collection hides truncated input. Pass `requireSizePrefix: false` to opt in:\n\n```dart\nfinal lenient = getArrayCodec(getU8Codec(), requireSizePrefix: false);\nlenient.decode(Uint8List(0)); // []\ngetArrayCodec(getU8Codec()).decode(Uint8List(0)); // throws\n```\n\n`Utf8CodecConfig` is accepted by `getUtf8Encoder`, `getUtf8Decoder`, and `getUtf8Codec`, and carries upstream's three options:\n\n- `fatal` rejects malformed input instead of replacing it. Upstream defaults to `false`, decoding bad bytes as `U+FFFD`; this port defaults to `true` and raises a `FormatException` from decoding and, for lone surrogates, from encoding.\n- `ignoreBOM` preserves a leading byte order mark. Both default to `false`, which strips it, matching Dart's `Utf8Decoder`.\n- `removeNullCharacters` strips `U+0000` from decoded strings. Upstream defaults to `true`; this port defaults to `false` so the decoded value reflects the bytes exactly.\n\nThe two divergent defaults exist so that malformed or null-padded account and instruction data cannot decode silently. To take upstream's behavior explicitly:\n\n```dart\nfinal codec = getUtf8Codec(\n  const Utf8CodecConfig(fatal: false, removeNullCharacters: true),\n);\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [cf4ab87](https://github.com/openbudgetfun/solana_kit/commit/cf4ab873e32bb2ce2f4fe69e73fbc8aa6162894b)",
              "#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Add the upstream size-prefix and UTF-8 codec options\n\nPorts the last two surfaces from upstream `@solana/kit` v8.3.0. Both are additive: no existing behavior changes.\n\n`requireSizePrefix` is a named parameter on `getArrayDecoder`, `getArrayCodec`, `getSetDecoder`, `getSetCodec`, `getMapDecoder`, and `getMapCodec`. Upstream defaults it to `false`, where an exhausted byte array decodes as an empty collection so a collection can be appended to an existing layout. This port defaults it to `true` and throws, because a silently empty collection hides truncated input. Pass `requireSizePrefix: false` to opt in:\n\n```dart\nfinal lenient = getArrayCodec(getU8Codec(), requireSizePrefix: false);\nlenient.decode(Uint8List(0)); // []\ngetArrayCodec(getU8Codec()).decode(Uint8List(0)); // throws\n```\n\n`Utf8CodecConfig` is accepted by `getUtf8Encoder`, `getUtf8Decoder`, and `getUtf8Codec`, and carries upstream's three options:\n\n- `fatal` rejects malformed input instead of replacing it. Upstream defaults to `false`, decoding bad bytes as `U+FFFD`; this port defaults to `true` and raises a `FormatException` from decoding and, for lone surrogates, from encoding.\n- `ignoreBOM` preserves a leading byte order mark. Both default to `false`, which strips it, matching Dart's `Utf8Decoder`.\n- `removeNullCharacters` strips `U+0000` from decoded strings. Upstream defaults to `true`; this port defaults to `false` so the decoded value reflects the bytes exactly.\n\nThe two divergent defaults exist so that malformed or null-padded account and instruction data cannot decode silently. To take upstream's behavior explicitly:\n\n```dart\nfinal codec = getUtf8Codec(\n  const Utf8CodecConfig(fatal: false, removeNullCharacters: true),\n);\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [cf4ab87](https://github.com/openbudgetfun/solana_kit/commit/cf4ab873e32bb2ce2f4fe69e73fbc8aa6162894b)\n\n#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)\n\n### Fixes\n\n#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)"
    },
    {
      "owner_id": "solana_kit_compute_budget",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_compute_budget/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_compute_budget [0.9.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_compute_budget/v0.9.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_compute_budget [0.9.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_compute_budget/v0.9.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_config",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_config/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_config [0.3.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_config/v0.3.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_config [0.3.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_config/v0.3.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_dapp_publisher_cli",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_dapp_publisher_cli/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_dapp_publisher_cli [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_dapp_publisher_cli/v0.2.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_dapp_publisher_cli [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_dapp_publisher_cli/v0.2.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_errors",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_errors/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)",
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Track @solana/kit v8.3.0\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Track @solana/kit v8.3.0\n\nThe workspace now tracks upstream `@solana/kit` v8.3.0 (previously v8.2.0), and `upstream:parity` passes against it. This entry maps every change in that upstream release to its Dart counterpart.\n\nPorted in this release:\n\n- `u256` and `i256` number codecs (`getU256Codec`, `getI256Codec` and their encoder/decoder pairs) serialize 32 bytes, honour the `endian` option, validate the full range on encode, and decode to `BigInt`, mirroring the existing 64-bit and 128-bit codecs.\n- Tap codec helpers observe values or bytes without modifying them: `tapEncoder`, `tapDecoder`, and `tapCodec` observe values, while `tapEncoderBytes`, `tapDecoderBytes`, and `tapCodecBytes` observe raw bytes and offsets. Each wrapper preserves the size characteristics of what it wraps, and any tap may throw, which makes them validation guards that need no identity `transformEncoder`.\n\nAlready present before this release, and now covered by the v8.3.0 claim:\n\n- The `getAgGenesisCert` RPC method and its allowed numeric keypaths.\n- `isSolanaRequest` recognising `getAgGenesisCert` and `getTransactionsForAddress`, which is also what fixed upstream's `bigint` parsing for `getTransactionsForAddress` responses.\n\nNo Dart change needed:\n\n- `HasAddress` and the `InstructionAccountInput` / `InstructionSignerInput` widening are TypeScript type-level changes. Dart has no structural typing, and this port's `ResolvedInstructionAccount` already wraps an `Object` value, so it accepts addresses, address-bearing objects, `ProgramDerivedAddress` values, and `AccountMeta` role overrides at runtime without a cast.\n- Marking `role` as `readonly` on the writable and signer account types is already true here: `AccountMeta.role` is a `final` field.\n- `createLazyKeyPairSignerFromBytes` exists upstream to defer an asynchronous WebCrypto key import. Signer creation in this port is synchronous, so there is nothing to defer and the type is not needed.\n\nVerification:\n\n- `upstream:parity` passes against `@solana/kit@8.3.0`\n- `upstream:check` reports the metadata is internally consistent for tracked version 8.3.0\n- The `@solana/kit` reference pin moved to tag `v8.3.0`\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [220066e](https://github.com/openbudgetfun/solana_kit/commit/220066ecb27aa738fd787ac8ada3918540435cc1)"
    },
    {
      "owner_id": "solana_kit_fast_stable_stringify",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_fast_stable_stringify/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_fixed_points",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_fixed_points/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)"
    },
    {
      "owner_id": "solana_kit_helius",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_helius/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_helius [0.7.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_helius/v0.7.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "- **Await the sender service call before disposing the signing keypair.** `buildTokenTransfer` now awaits `sendViaSender` inside the `try` block instead of returning the Future directly, so the ephemeral keypair is disposed after the send completes rather than at return time. Signing already finishes before the call, so this is a lifecycle hygiene change with no observable behavior difference; it satisfies the Dart 3.13 `unawaited_return_in_try_block` diagnostic that the Flutter 3.47 toolchain enables.\n  _Owner:_ Ifiok Jr. · _Introduced in:_ [e041e73](https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_helius [0.7.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_helius/v0.7.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n- **Await the sender service call before disposing the signing keypair.** `buildTokenTransfer` now awaits `sendViaSender` inside the `try` block instead of returning the Future directly, so the ephemeral keypair is disposed after the send completes rather than at return time. Signing already finishes before the call, so this is a lifecycle hygiene change with no observable behavior difference; it satisfies the Dart 3.13 `unawaited_return_in_try_block` diagnostic that the Flutter 3.47 toolchain enables.\n  _Owner:_ Ifiok Jr. · _Introduced in:_ [e041e73](https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1)"
    },
    {
      "owner_id": "solana_kit_instruction_plans",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_instruction_plans/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_instructions",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_instructions/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_integration_tests",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_integration_tests/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_integration_tests [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_integration_tests/v0.2.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Track the memo v4 program and add memo extraction helpers\n\nThe workspace now tracks `solana-program/memo` at `js@v0.14.1` (previously `js@v0.13.1`). The `mpl-token-metadata` reference pin also moves to `353d01be4af3`; its IDL is byte-identical, so that package is unaffected.\n\n`solana_kit_address_constants` moves `memoProgramAddress` to the v4 memo program (`Memo4c2pN8afCj432Lb7RMVKi9PbQnnW7ewFFaV3oAH`), matching the upstream IDL `publicKey` as of `js@v0.14.0`. The previous v3 address stays available as the new `memoLegacyProgramAddressV3` constant, and `memoLegacyProgramAddress` (v1) is unchanged. Code that builds new memo instructions picks up the v4 program automatically; code that must keep targeting v3 names the legacy constant explicitly.\n\n`solana_kit_memo` ports the upstream extraction helpers from `js@v0.14.1`:\n\n- `getMemosFromInstructions` scans a list of instructions, matches every deployed Memo program address, and returns the UTF-8 decoded memo text with the raw bytes, source program address, and instruction index.\n- `ExtractedMemo` carries one extracted memo.\n- `supportedMemoProgramAddresses` lists every deployed Memo program address ordered from oldest (v1) to newest (v4).\n\nThe generated layer is regenerated against `js@v0.14.1` with the current renderer: the program page is byte-identical because the program address flows through the well-known constants, and the AddMemo data decoder now validates byte length strictly, throwing `SolanaError` with `codecsInvalidByteLength` on trailing bytes. `solana_kit_errors` moves from a dev dependency to a dependency because the generated decoder references it.\n\n`codama-renderers-dart` maps the v4 address to `memoProgramAddress` and the v3 address to `memoLegacyProgramAddressV3` in its well-known address registry, so regenerated clients re-export the canonical constants instead of hardcoding address strings.\n\nBuild new memo instructions against `memoProgramAddress` (v4). Where a memo must be executed by a program the runtime provides, check what is deployed: SurfPool, used by this repository's on-chain integration tests, ships the v1 and v3 programs as executable bytecode but resolves v4 to a placeholder, so those tests invoke `memoLegacyProgramAddressV3`. All three programs share the same instruction format, so only the program the transaction targets differs.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)",
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_integration_tests [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_integration_tests/v0.2.0) (2026-09-21)\n\n### Fixes\n\n#### Track the memo v4 program and add memo extraction helpers\n\nThe workspace now tracks `solana-program/memo` at `js@v0.14.1` (previously `js@v0.13.1`). The `mpl-token-metadata` reference pin also moves to `353d01be4af3`; its IDL is byte-identical, so that package is unaffected.\n\n`solana_kit_address_constants` moves `memoProgramAddress` to the v4 memo program (`Memo4c2pN8afCj432Lb7RMVKi9PbQnnW7ewFFaV3oAH`), matching the upstream IDL `publicKey` as of `js@v0.14.0`. The previous v3 address stays available as the new `memoLegacyProgramAddressV3` constant, and `memoLegacyProgramAddress` (v1) is unchanged. Code that builds new memo instructions picks up the v4 program automatically; code that must keep targeting v3 names the legacy constant explicitly.\n\n`solana_kit_memo` ports the upstream extraction helpers from `js@v0.14.1`:\n\n- `getMemosFromInstructions` scans a list of instructions, matches every deployed Memo program address, and returns the UTF-8 decoded memo text with the raw bytes, source program address, and instruction index.\n- `ExtractedMemo` carries one extracted memo.\n- `supportedMemoProgramAddresses` lists every deployed Memo program address ordered from oldest (v1) to newest (v4).\n\nThe generated layer is regenerated against `js@v0.14.1` with the current renderer: the program page is byte-identical because the program address flows through the well-known constants, and the AddMemo data decoder now validates byte length strictly, throwing `SolanaError` with `codecsInvalidByteLength` on trailing bytes. `solana_kit_errors` moves from a dev dependency to a dependency because the generated decoder references it.\n\n`codama-renderers-dart` maps the v4 address to `memoProgramAddress` and the v3 address to `memoLegacyProgramAddressV3` in its well-known address registry, so regenerated clients re-export the canonical constants instead of hardcoding address strings.\n\nBuild new memo instructions against `memoProgramAddress` (v4). Where a memo must be executed by a program the runtime provides, check what is deployed: SurfPool, used by this repository's on-chain integration tests, ships the v1 and v3 programs as executable bytecode but resolves v4 to a placeholder, so those tests invoke `memoLegacyProgramAddressV3`. All three programs share the same instruction format, so only the program the transaction targets differs.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_jupiter",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_jupiter/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
    },
    {
      "owner_id": "solana_kit_keys",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_keys/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_lints",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_lints/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_loader",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_loader/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_loader [0.5.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_loader/v0.5.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_loader [0.5.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_loader/v0.5.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_memo",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_memo/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_memo [0.5.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_memo/v0.5.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Track the memo v4 program and add memo extraction helpers\n\nThe workspace now tracks `solana-program/memo` at `js@v0.14.1` (previously `js@v0.13.1`). The `mpl-token-metadata` reference pin also moves to `353d01be4af3`; its IDL is byte-identical, so that package is unaffected.\n\n`solana_kit_address_constants` moves `memoProgramAddress` to the v4 memo program (`Memo4c2pN8afCj432Lb7RMVKi9PbQnnW7ewFFaV3oAH`), matching the upstream IDL `publicKey` as of `js@v0.14.0`. The previous v3 address stays available as the new `memoLegacyProgramAddressV3` constant, and `memoLegacyProgramAddress` (v1) is unchanged. Code that builds new memo instructions picks up the v4 program automatically; code that must keep targeting v3 names the legacy constant explicitly.\n\n`solana_kit_memo` ports the upstream extraction helpers from `js@v0.14.1`:\n\n- `getMemosFromInstructions` scans a list of instructions, matches every deployed Memo program address, and returns the UTF-8 decoded memo text with the raw bytes, source program address, and instruction index.\n- `ExtractedMemo` carries one extracted memo.\n- `supportedMemoProgramAddresses` lists every deployed Memo program address ordered from oldest (v1) to newest (v4).\n\nThe generated layer is regenerated against `js@v0.14.1` with the current renderer: the program page is byte-identical because the program address flows through the well-known constants, and the AddMemo data decoder now validates byte length strictly, throwing `SolanaError` with `codecsInvalidByteLength` on trailing bytes. `solana_kit_errors` moves from a dev dependency to a dependency because the generated decoder references it.\n\n`codama-renderers-dart` maps the v4 address to `memoProgramAddress` and the v3 address to `memoLegacyProgramAddressV3` in its well-known address registry, so regenerated clients re-export the canonical constants instead of hardcoding address strings.\n\nBuild new memo instructions against `memoProgramAddress` (v4). Where a memo must be executed by a program the runtime provides, check what is deployed: SurfPool, used by this repository's on-chain integration tests, ships the v1 and v3 programs as executable bytecode but resolves v4 to a placeholder, so those tests invoke `memoLegacyProgramAddressV3`. All three programs share the same instruction format, so only the program the transaction targets differs.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_memo [0.5.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_memo/v0.5.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Track the memo v4 program and add memo extraction helpers\n\nThe workspace now tracks `solana-program/memo` at `js@v0.14.1` (previously `js@v0.13.1`). The `mpl-token-metadata` reference pin also moves to `353d01be4af3`; its IDL is byte-identical, so that package is unaffected.\n\n`solana_kit_address_constants` moves `memoProgramAddress` to the v4 memo program (`Memo4c2pN8afCj432Lb7RMVKi9PbQnnW7ewFFaV3oAH`), matching the upstream IDL `publicKey` as of `js@v0.14.0`. The previous v3 address stays available as the new `memoLegacyProgramAddressV3` constant, and `memoLegacyProgramAddress` (v1) is unchanged. Code that builds new memo instructions picks up the v4 program automatically; code that must keep targeting v3 names the legacy constant explicitly.\n\n`solana_kit_memo` ports the upstream extraction helpers from `js@v0.14.1`:\n\n- `getMemosFromInstructions` scans a list of instructions, matches every deployed Memo program address, and returns the UTF-8 decoded memo text with the raw bytes, source program address, and instruction index.\n- `ExtractedMemo` carries one extracted memo.\n- `supportedMemoProgramAddresses` lists every deployed Memo program address ordered from oldest (v1) to newest (v4).\n\nThe generated layer is regenerated against `js@v0.14.1` with the current renderer: the program page is byte-identical because the program address flows through the well-known constants, and the AddMemo data decoder now validates byte length strictly, throwing `SolanaError` with `codecsInvalidByteLength` on trailing bytes. `solana_kit_errors` moves from a dev dependency to a dependency because the generated decoder references it.\n\n`codama-renderers-dart` maps the v4 address to `memoProgramAddress` and the v3 address to `memoLegacyProgramAddressV3` in its well-known address registry, so regenerated clients re-export the canonical constants instead of hardcoding address strings.\n\nBuild new memo instructions against `memoProgramAddress` (v4). Where a memo must be executed by a program the runtime provides, check what is deployed: SurfPool, used by this repository's on-chain integration tests, ships the v1 and v3 programs as executable bytecode but resolves v4 to a placeholder, so those tests invoke `memoLegacyProgramAddressV3`. All three programs share the same instruction format, so only the program the transaction targets differs.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)\n\n### Fixes\n\n#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)"
    },
    {
      "owner_id": "solana_kit_mobile_wallet_adapter",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_mobile_wallet_adapter/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_mobile_wallet_adapter [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_mobile_wallet_adapter/v0.6.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "- **Refresh analyzer excludes for the Flutter 3.47 toolchain.** The Flutter 3.47 tooling rewrites package `analysis_options.yaml` files during resolution to exclude generated `build/**` output from analysis. This applies the same exclusion to the wallet adapter and mobile wallet adapter packages (and the mobile example), keeping their analysis options stable under the updated FVM pin. No public API or behavior changes.\n  _Owner:_ Ifiok Jr. · _Introduced in:_ [e041e73](https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_mobile_wallet_adapter [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_mobile_wallet_adapter/v0.6.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n- **Refresh analyzer excludes for the Flutter 3.47 toolchain.** The Flutter 3.47 tooling rewrites package `analysis_options.yaml` files during resolution to exclude generated `build/**` output from analysis. This applies the same exclusion to the wallet adapter and mobile wallet adapter packages (and the mobile example), keeping their analysis options stable under the updated FVM pin. No public API or behavior changes.\n  _Owner:_ Ifiok Jr. · _Introduced in:_ [e041e73](https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1)"
    },
    {
      "owner_id": "solana_kit_mobile_wallet_adapter_protocol",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_mobile_wallet_adapter_protocol/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_mobile_wallet_adapter_protocol [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_mobile_wallet_adapter_protocol/v0.6.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_mobile_wallet_adapter_protocol [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_mobile_wallet_adapter_protocol/v0.6.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_mpl_bubblegum",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_mpl_bubblegum/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_mpl_bubblegum [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_mpl_bubblegum/v0.6.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_mpl_bubblegum [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_mpl_bubblegum/v0.6.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_mpl_core",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_mpl_core/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
    },
    {
      "owner_id": "solana_kit_mpl_token_metadata",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_mpl_token_metadata/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
    },
    {
      "owner_id": "solana_kit_offchain_messages",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_offchain_messages/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_offchain_messages [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_offchain_messages/v0.6.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_offchain_messages [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_offchain_messages/v0.6.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Align error code numbers with upstream and report malformed UTF-8 as a code\n\n`SolanaErrorCode` numbers now match upstream `@solana/kit` exactly. Two port-only codes were occupying numbers upstream uses for its UTF-8 codes, which made any cross-SDK comparison of those numbers wrong.\n\nThe UTF-8 codec also stops raising a bare `FormatException` and reports through the error codes upstream defines:\n\n- `codecsInvalidUtf8Bytes` (`8078026`) for a malformed byte sequence, carrying the `offset` where decoding failed.\n- `codecsInvalidUtf8String` (`8078027`) for a lone surrogate, carrying its `index`. This is thrown when encoding with `fatal: true` and, with `fatal: false`, both directions keep replacing the offending unit with `U+FFFD`.\n\nTwo port-only codes moved or went away:\n\n- `codecsInvalidBoolean` moved from `8078027` to `8078999`. The number had to change because `8078027` is upstream's `CODECS__INVALID_UTF8_STRING`. This port validates that booleans are encoded as `0` or `1` and upstream does not, so the code has no upstream counterpart and now sits at the end of the codec block, where upstream cannot collide with it. If you match on `SolanaErrorCode.codecsInvalidBoolean.value`, update the number; matching on the enum member is unaffected.\n- `codecsStringContainsNullCharacters` was removed. Nothing in the workspace threw it, and upstream has no equivalent. Use `Utf8CodecConfig.removeNullCharacters` to control null handling instead.\n\n```dart\ntry {\n  getUtf8Decoder().decode(bytes);\n} on SolanaError catch (error) {\n  if (error.code == SolanaErrorCode.codecsInvalidUtf8Bytes) {\n    print('bad bytes at ${error.context['offset']}');\n  }\n}\n```\n\n`solana_kit_memo`, `solana_kit_offchain_messages`, and `solana_kit_attestation_service` only had tests asserting the old `FormatException` for malformed UTF-8; those assertions now check the error code, and `solana_kit_memo` declares the `solana_kit_errors` dev dependency that needs.\n\n`upstream:error-codes`, which also runs as part of `docs:check`, compares this enum against `.repos/kit/packages/errors/src/codes.ts` and fails on a number mismatch or an occupied number, so this cannot drift again without CI saying so.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [a8f643f](https://github.com/openbudgetfun/solana_kit/commit/a8f643f1ae7e6594fbfa972d473f7042b1f6ace0)"
    },
    {
      "owner_id": "solana_kit_options",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_options/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_program_client_core",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_program_client_core/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_programs",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_programs/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_pyth",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_pyth/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
    },
    {
      "owner_id": "solana_kit_rpc",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_rpc/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_rpc_api",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_rpc_api/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_rpc_parsed_types",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_rpc_parsed_types/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_rpc_spec",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_rpc_spec/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_rpc_spec_types",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_rpc_spec_types/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)"
    },
    {
      "owner_id": "solana_kit_rpc_subscriptions",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_rpc_subscriptions/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_rpc_subscriptions_api",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_rpc_subscriptions_api/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_rpc_subscriptions_channel_websocket",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_rpc_subscriptions_channel_websocket/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_rpc_transformers",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_rpc_transformers/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_rpc_transport_http",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_rpc_transport_http/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_rpc_types",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_rpc_types/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_signers",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_signers/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_sns",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_sns/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)",
              "#### Verify both hand-written SHA-256 implementations against an independent hash\n\n`solana_kit_anchor` and `solana_kit_sns` each carry their own pure-Dart SHA-256. Anchor uses it to derive program discriminators and SNS to derive name account addresses, so a defect in either changes which instruction or account a program resolves to — a wrong result rather than an exception. Both were covered only by a handful of fixed NIST vectors, which pin the round function but rarely land on the padding and block boundaries where a length bug hides.\n\nEach package now has a `sha256_differential_test.dart` comparing its implementation against `package:crypto` (the same dependency `solana_kit_addresses` already uses). Coverage includes every input length from 0 to 200, which crosses the 55/56-byte padding transitions and the 64-byte block boundary, lengths from 255 through 10000 for multi-block behavior, randomized inputs, and all-zero and all-`0xff` buffers. Anchor additionally checks `instructionDiscriminator` against the truncated namespaced digest, and SNS checks `getHashedName` against `sha256(\"SPL Name Service\" + name)`.\n\nBoth implementations agree with the reference on every case. No library behavior changes.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)\n\n#### Verify both hand-written SHA-256 implementations against an independent hash\n\n`solana_kit_anchor` and `solana_kit_sns` each carry their own pure-Dart SHA-256. Anchor uses it to derive program discriminators and SNS to derive name account addresses, so a defect in either changes which instruction or account a program resolves to — a wrong result rather than an exception. Both were covered only by a handful of fixed NIST vectors, which pin the round function but rarely land on the padding and block boundaries where a length bug hides.\n\nEach package now has a `sha256_differential_test.dart` comparing its implementation against `package:crypto` (the same dependency `solana_kit_addresses` already uses). Coverage includes every input length from 0 to 200, which crosses the 55/56-byte padding transitions and the 64-byte block boundary, lengths from 255 through 10000 for multi-block behavior, randomized inputs, and all-zero and all-`0xff` buffers. Anchor additionally checks `instructionDiscriminator` against the truncated namespaced digest, and SNS checks `getHashedName` against `sha256(\"SPL Name Service\" + name)`.\n\nBoth implementations agree with the reference on every case. No library behavior changes.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)"
    },
    {
      "owner_id": "solana_kit_spl_account_compression",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_spl_account_compression/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_spl_account_compression [0.5.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_spl_account_compression/v0.5.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_spl_account_compression [0.5.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_spl_account_compression/v0.5.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_squads",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_squads/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Restore complete version inventories in package READMEs\n\nThe `versions.json` data source that renders every package README's installation section had drifted: packages first released after the legacy knope era were never added, so their READMEs told consumers to depend on a bare `^` with no version. The retired `solana_kit_functional` entry and a stale `solana_kit_mobile_wallet_adapter_example` version were also lingering.\n\nThe inventory now matches every package's `pubspec.yaml`, the `solana_kit_functional` key is gone, and the affected installation sections render the real published version again:\n\n```yaml\ndependencies:\n  \"solana_kit_jupiter\": ^0.9.3\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [4c8855b](https://github.com/openbudgetfun/solana_kit/commit/4c8855bd608bb9f05324b001b552c9329679e441)"
    },
    {
      "owner_id": "solana_kit_stake",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_stake/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_stake [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_stake/v0.6.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_stake [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_stake/v0.6.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_subscribable",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_subscribable/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_subscriptions",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_subscriptions/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_subscriptions [0.3.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_subscriptions/v0.3.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_subscriptions [0.3.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_subscriptions/v0.3.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_surfpool",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_surfpool/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_surfpool [0.4.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_surfpool/v0.4.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_surfpool [0.4.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_surfpool/v0.4.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_system",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_system/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_system [0.8.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_system/v0.8.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_system [0.8.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_system/v0.8.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_sysvars",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_sysvars/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_sysvars [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_sysvars/v0.6.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_sysvars [0.6.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_sysvars/v0.6.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_test_matchers",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_test_matchers/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Fixes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_token",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_token/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_token [0.9.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_token/v0.9.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_token [0.9.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_token/v0.9.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_token_2022",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_token_2022/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "solana_kit_token_2022 [0.9.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_token_2022/v0.9.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)",
              "#### Track Token-2022 js@v0.18.0\n\n`solana_kit_token_2022` follows `solana-program/token-2022` to `js@v0.18.0`, up from `js@v0.16.1`. The pin had been held back because the migration needed handwritten-layer work; that work is now done.\n\nThe generated extension union renames its variants to the renderer's current convention, prefixing each with the union name: `TransferFeeConfig` becomes `ExtensionTransferFeeConfig`, `MetadataPointer` becomes `ExtensionMetadataPointer`, and so on for all twenty-nine variants. The prefix keeps variants readable at the call site and stops them from colliding with same-named generated types. Update pattern matches and constructors:\n\n```dart\nfinal instructions = getPreInitializeInstructionsForMintExtensions(\n  mint: mintAddress,\n  extensions: [\n    ExtensionTransferFeeConfig(\n      transferFeeConfigAuthority: authority,\n      withdrawWithheldAuthority: authority,\n      withheldAmount: BigInt.zero,\n      olderTransferFee: olderFee,\n      newerTransferFee: newerFee,\n    ),\n  ],\n);\n```\n\nMint and token accounts now decode their TLV extension region the way the program reads it. The previous codec decoded the region as a `remainder` array, which had to consume every trailing byte as an entry, so an account allocated with unused space either threw or reported the padding as a spurious `Uninitialized` extension. The new codec walks entries until it meets an `Uninitialized` (type 0) header or fewer than two bytes remain, and ignores whatever follows, matching the program and `@solana/spl-token`:\n\n```dart\nfinal account = getMintDecoder().decode(bytesWithTwoBytesOfPadding);\n// extensions now holds only the real entries, never the padding.\n```\n\n`solana_kit_address_constants`-style address handling is unchanged; `getMintSize` and `getTokenSize` keep their existing signatures and results, and now share the same encoder the accounts use, so a size computed from a list of extensions always matches what the encoder writes.\n\n`codama-renderers-dart` gains `linkOverrides`, which lets an IDL link be backed by a hand-written Dart codec while the wrappers around it (`Option`, `HiddenPrefix`, …) still come from the IDL. That is what the Token-2022 extension region uses, and it is the extension point for any future type Codama cannot express. The renderer also stops requiring an account to match its size discriminator exactly: a size discriminator describes an account's fixed prefix, so accounts with variable trailing fields are legitimately longer. Instruction sizes are still matched exactly, since there the size identifies the whole payload.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)"
            ]
          }
        ]
      },
      "rendered": "## solana_kit_token_2022 [0.9.0](https://github.com/openbudgetfun/solana_kit/releases/tag/solana_kit_token_2022/v0.9.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n#### Track Token-2022 js@v0.18.0\n\n`solana_kit_token_2022` follows `solana-program/token-2022` to `js@v0.18.0`, up from `js@v0.16.1`. The pin had been held back because the migration needed handwritten-layer work; that work is now done.\n\nThe generated extension union renames its variants to the renderer's current convention, prefixing each with the union name: `TransferFeeConfig` becomes `ExtensionTransferFeeConfig`, `MetadataPointer` becomes `ExtensionMetadataPointer`, and so on for all twenty-nine variants. The prefix keeps variants readable at the call site and stops them from colliding with same-named generated types. Update pattern matches and constructors:\n\n```dart\nfinal instructions = getPreInitializeInstructionsForMintExtensions(\n  mint: mintAddress,\n  extensions: [\n    ExtensionTransferFeeConfig(\n      transferFeeConfigAuthority: authority,\n      withdrawWithheldAuthority: authority,\n      withheldAmount: BigInt.zero,\n      olderTransferFee: olderFee,\n      newerTransferFee: newerFee,\n    ),\n  ],\n);\n```\n\nMint and token accounts now decode their TLV extension region the way the program reads it. The previous codec decoded the region as a `remainder` array, which had to consume every trailing byte as an entry, so an account allocated with unused space either threw or reported the padding as a spurious `Uninitialized` extension. The new codec walks entries until it meets an `Uninitialized` (type 0) header or fewer than two bytes remain, and ignores whatever follows, matching the program and `@solana/spl-token`:\n\n```dart\nfinal account = getMintDecoder().decode(bytesWithTwoBytesOfPadding);\n// extensions now holds only the real entries, never the padding.\n```\n\n`solana_kit_address_constants`-style address handling is unchanged; `getMintSize` and `getTokenSize` keep their existing signatures and results, and now share the same encoder the accounts use, so a size computed from a list of extensions always matches what the encoder writes.\n\n`codama-renderers-dart` gains `linkOverrides`, which lets an IDL link be backed by a hand-written Dart codec while the wrappers around it (`Option`, `HiddenPrefix`, …) still come from the IDL. That is what the Token-2022 extension region uses, and it is the extension point for any future type Codama cannot express. The renderer also stops requiring an account to match its size discriminator exactly: a size discriminator describes an account's fixed prefix, so accounts with variable trailing fields are legitimately longer. Instruction sizes are still matched exactly, since there the size identifies the whole payload.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [9dcb59a](https://github.com/openbudgetfun/solana_kit/commit/9dcb59a09c4b13fc471a286612da570a8141e3c7)"
    },
    {
      "owner_id": "solana_kit_transaction_confirmation",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_transaction_confirmation/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_transaction_introspection",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_transaction_introspection/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_transaction_messages",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_transaction_messages/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Reject truncated numeric reads with `SolanaError` instead of `RangeError`\n\nA decoder reading a fixed-width field out of malformed wire data could raise a raw `RangeError` or `IndexError` out of the SDK instead of the documented `SolanaError`, so a caller catching `SolanaError` saw an escaped exception rather than a rejection it could handle. A truncated account, transaction, or RPC payload reaching the decoders was enough to trigger it; no signature or cluster access was required.\n\nThe guard that upstream `@solana/kit` applies in its number decoder factory was missing from three ports of it. `numberDecoderFactory` and `floatDecoderFactory` read through a `ByteData` view without first checking that the requested width was available, and the six `BigInt` decoders (`u64`, `i64`, `u128`, `i128`, `u256`, `i256`) indexed bytes directly with the same gap. All eight now call `assertByteArrayIsNotEmptyForCodec` and `assertByteArrayHasEnoughBytesForCodec` before reading, raising `codecsCannotDecodeEmptyByteArray` or `codecsInvalidByteLength` as upstream does. A new `bigIntDecoderFactory` carries the guard for the multi-word widths.\n\nTwo further escape sites in `solana_kit_transaction_messages` are fixed. The version 1 instruction payload slice computed `pos + numInstructionDataBytes` and sliced without confirming the buffer held that many bytes; it now asserts the length the way upstream's `fixDecoderSize` wrapper does. The transaction version decoder read `bytes[offset]` unguarded — upstream tolerates this because JavaScript yields `undefined`, which then silently takes the legacy branch and misreports a truncated buffer as an unversioned message, so this port rejects the empty buffer rather than reproducing that fallback.\n\nEncoders are unchanged and still raise `RangeError` when a destination buffer is too small. Upstream writes into a scratch buffer and then `bytes.set`s it, which throws in JavaScript too, so that behavior is deliberate parity rather than a defect.\n\nCallers that caught `RangeError` around a decode should catch `SolanaError`. Callers that already caught `SolanaError` now see malformed input rejected where it previously surfaced as a crash.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)",
              "#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Replace stubbed functions with real implementations\n\nSeveral public functions promised behavior they did not deliver. Each is now implemented, with the missing API surface added alongside it.\n\n##### `estimateResourceLimitsFactory` now simulates\n\nThe previous implementation returned its argument unchanged, so it performed no simulation: no compute unit measurement, no loaded accounts data size, no failure reporting. It also could not have worked where it lived, because it needs an RPC client and the transaction compiler, and `solana_kit_transaction_messages` depends on neither. Upstream defines this function in the umbrella `@solana/kit` package for the same reason, so it now lives in `package:solana_kit` with that dependency available.\n\nIt takes an `EstimateResourceLimitsFactoryConfig` holding the RPC client and returns a function that:\n\n- Sets the compute unit limit to the maximum (`1400000`) and, for version 1 messages, the loaded accounts data size limit to the maximum (`67108864`) before simulating, so the simulation is not cut short by a resource ceiling.\n- Asks the node to replace the blockhash for blockhash-lifetime transactions, and uses the real nonce for durable nonce transactions.\n- Returns the `unitsConsumed` the node reported, capped at the `u32` ceiling, plus `loadedAccountsDataSize`.\n- Throws `transactionFailedToEstimateComputeLimit` when the node reports no compute units, `transactionFailedToEstimateLoadedAccountsDataSizeLimit` when a version 1 simulation omits the loaded accounts size, and `transactionFailedWhenSimulatingToEstimateResourceLimits` with the decoded transaction error as `cause` when the transaction itself fails. All three codes already existed and were never thrown.\n\nThree supporting pieces land with it:\n\n- `simulateTransaction` and its `simulateTransactionValue` result are now available on the RPC client. The method was reachable only by hand-assembling a request before this.\n- `maxLoadedAccountsDataSizeLimit` (`67108864`) is exported.\n- `estimateAndSetResourceLimitsFactory` no longer computes a loaded accounts data size for legacy and version 0 messages. It previously did, which spent an extra simulation and could attach a `SetLoadedAccountsDataSizeLimit` instruction the runtime ignores. The loaded accounts limit is now only ever set on version 1 messages, matching upstream.\n\n```dart\nfinal estimate = estimateResourceLimitsFactory(\n  EstimateResourceLimitsFactoryConfig(rpc: rpc),\n);\nfinal withLimits = await estimateAndSetResourceLimitsFactory(estimate)(message);\n```\n\n##### `solana_kit_functional` removed\n\nThe package is gone. Its only utility, the `pipe` extension, has lived in `solana_kit_transaction_messages` since the previous breaking release and is re-exported by `solana_kit`, so the package duplicated what the SDK already provided and existed only as an empty placeholder pending retirement. Anyone still importing it should switch to `solana_kit_transaction_messages` (or the `solana_kit` umbrella), which is a one-line import change.\n\n##### `solana_kit_addresses` gains the PDA guards\n\n`isProgramDerivedAddress` and `assertIsProgramDerivedAddress` were absent, leaving `addressesMalformedPda` and `addressesPdaBumpSeedOutOfRange` defined but unreachable. Both are now implemented: they validate that a value is an `(Address, int)` record, that the bump seed is in `[0, 255]`, and that the address is well formed.\n\n##### `solana_kit_helius` builds real smart transactions\n\n`createSmartTransaction` returned a bare blockhash while documenting that it would estimate compute units and priority fees. It now performs the full sequence: validate, estimate compute units through `simulateTransaction`, sample the priority fee by account key, resolve the fee in both microLamports-per-unit and total lamports, and refresh the blockhash. It returns a `SmartTransaction` carrying the limits, fee, lifetime, instructions, fee payer, and account keys; signing stays with the caller because the client holds signer addresses rather than keys.\n\nTwo related silent defaults were removed:\n\n- `getComputeUnits` returned `200000` when the node omitted `unitsConsumed`. It now throws, because inventing a number sizes the transaction for work the simulation never confirmed. It also reports a failed simulation instead of returning the units of one that did not succeed, and it serializes real `Instruction` objects, which previously failed at JSON encoding.\n- `broadcastTransaction`, `sendTransactionWithSender`, and `sendSmartTransaction` accepted a `senderUrl` parameter they never used. The parameter is gone; the REST client already targets the sender base URL.\n\n##### Version 1 durable nonce transactions are recognized\n\n`getTransactionLifetimeConstraintFromCompiledTransactionMessage` only inspected the legacy instruction list, which a version 1 compiled message leaves empty in favour of separate instruction headers and payloads. Every version 1 durable nonce transaction therefore decompiled as a blockhash transaction, so a caller could not tell that its lifetime depended on a nonce. The version 1 branch now reads the headers and payloads, throws `transactionInvalidNonceAccountIndex` for an out-of-range nonce account index, and returns the blockhash lifetime only when the first instruction is genuinely not an advance-nonce instruction.\n\n##### Priority fee lamports API\n\n`getTransactionMessagePriorityFeeLamports` and `setTransactionMessagePriorityFeeLamports` add the missing read/write surface for the total-lamport priority fee that only version 1 messages carry. The setter removes the fee on `null`, drops an emptied config, and is a no-op when the value already matches.\n\n##### Wallets can now express and check version 1 support\n\n`SolanaTransactionVersion` gains `version1` plus `wireValue` and `fromWireValue`, so the values a wallet advertises (`legacy`, `0`, `1`) round-trip instead of being collapsed. A `supportsVersion1` extension makes the check usable. Two related corrections:\n\n- The browser registry used to map any advertised entry other than `legacy` onto version 0, so a wallet advertising `1` was reported as version 0 and a caller could build a transaction the wallet cannot sign. Unrecognized entries are now dropped rather than mislabelled.\n- The MWA-backed mobile wallet advertises an explicit `legacy`-and-version-0 list instead of `SolanaTransactionVersion.values`, which would have silently started claiming version 1 support as the enum grew. This matches upstream's `wallet-standard-mobile`.\n\n##### Error codes that were defined but unreachable\n\nThree codes had no throw site. `signerWalletAccountCannotSignTransaction` is now thrown when a `WalletAccountSigner` is created for an account advertising neither transaction feature, matching upstream's `createSignerFromWalletAccount`. `heliusApiKeyRequired` is thrown by `HeliusConfig` for a blank key, which previously produced a request that could only fail with a 401. `heliusTransactionSimulationFailed` replaces a bare `StateError` when a compute-unit simulation reports a transaction failure.\n\nThe remaining defined-but-unthrown codes were checked against upstream and are parity-faithful: upstream defines them without throwing them anywhere either (`addressesInvalidBase58EncodedAddress`, the four `wallet*` codes, `subscribableRetryNotSupported`, `transactionInvalidNonceTransactionFirstInstructionMustBeAdvanceNonce`), or they belong to abstractions this port intentionally does not have (the React hook path behind `signerWalletMultisignUnimplemented`, the fs-impl package behind `fsUnsupportedEnvironment`, the named-channel pubsub plan behind `invariantViolationDataPublisherChannelUnimplemented`).\n\n##### `solana_kit_dapp_publisher_cli` reports unreadable balances\n\n`parseLamportsValue` returned `0` for a balance response it could not parse. A malformed response therefore looked like an empty wallet. It now throws a `FormatException`, so a transport or schema change is reported as itself rather than as insufficient funds.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Reject truncated numeric reads with `SolanaError` instead of `RangeError`\n\nA decoder reading a fixed-width field out of malformed wire data could raise a raw `RangeError` or `IndexError` out of the SDK instead of the documented `SolanaError`, so a caller catching `SolanaError` saw an escaped exception rather than a rejection it could handle. A truncated account, transaction, or RPC payload reaching the decoders was enough to trigger it; no signature or cluster access was required.\n\nThe guard that upstream `@solana/kit` applies in its number decoder factory was missing from three ports of it. `numberDecoderFactory` and `floatDecoderFactory` read through a `ByteData` view without first checking that the requested width was available, and the six `BigInt` decoders (`u64`, `i64`, `u128`, `i128`, `u256`, `i256`) indexed bytes directly with the same gap. All eight now call `assertByteArrayIsNotEmptyForCodec` and `assertByteArrayHasEnoughBytesForCodec` before reading, raising `codecsCannotDecodeEmptyByteArray` or `codecsInvalidByteLength` as upstream does. A new `bigIntDecoderFactory` carries the guard for the multi-word widths.\n\nTwo further escape sites in `solana_kit_transaction_messages` are fixed. The version 1 instruction payload slice computed `pos + numInstructionDataBytes` and sliced without confirming the buffer held that many bytes; it now asserts the length the way upstream's `fixDecoderSize` wrapper does. The transaction version decoder read `bytes[offset]` unguarded — upstream tolerates this because JavaScript yields `undefined`, which then silently takes the legacy branch and misreports a truncated buffer as an unversioned message, so this port rejects the empty buffer rather than reproducing that fallback.\n\nEncoders are unchanged and still raise `RangeError` when a destination buffer is too small. Upstream writes into a scratch buffer and then `bytes.set`s it, which throws in JavaScript too, so that behavior is deliberate parity rather than a defect.\n\nCallers that caught `RangeError` around a decode should catch `SolanaError`. Callers that already caught `SolanaError` now see malformed input rejected where it previously surfaced as a crash.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)\n\n#### Speed up base-X codecs, BigInt JSON parsing, and fixed-point parsing\n\nSame results, less work per call. No public API, output byte, or error behavior changed; `upstream:parity` passes against `@solana/kit@8.3.0` and the full workspace suite is green.\n\nThe base-X codecs converted through `BigInt`, scaled the alphabet with `String.indexOf` and a fresh one-character string per input character, and built output with repeated `insert(0, …)` calls that reallocate and shift the whole list each time. Encoding a typical base58 address cost roughly 600 µs, so compiling a 20-account transaction spent measurable milliseconds on address encoding alone. The conversion now folds digits into a byte buffer with word-sized carry arithmetic, indexes the alphabet through a cached code-unit lookup, and writes output in order:\n\n```dart\n// Before: BigInt division per character plus O(n²) list inserts.\n// After: one carry pass per character over a preallocated buffer.\nfinal converted = _convertToBytes(value, alphabet);\nbytes\n  ..fillRange(offset, offset + converted.leadingZeroes, 0)\n  ..setAll(offset + converted.leadingZeroes, converted.bytes);\n```\n\nMeasured on an interleaved best-of-N benchmark, with the previous implementation running in the same process to cancel machine noise:\n\n| Operation                        | Before   | After   | Improvement |\n| -------------------------------- | -------- | ------- | ----------- |\n| base58 encode, typical address   | 22.97 µs | 2.88 µs | 8.0x        |\n| base58 decode, 32 bytes          | 16.79 µs | 3.24 µs | 5.2x        |\n| base58 encode, 64-byte signature | 52.89 µs | 8.44 µs | 6.3x        |\n\n`parseJsonWithBigInts` allocated a one-character string and ran up to two regular expressions per character of the payload, then rebuilt the document character by character. It now scans by code unit, copies literal runs as substrings, and hoists the exponent pattern:\n\n| Operation                 | Before   | After    | Improvement |\n| ------------------------- | -------- | -------- | ----------- |\n| parse a 28 KB RPC payload | 2.198 ms | 1.089 ms | 2.0x        |\n\nAlso removed in the same pass: a redundant full copy in the UTF-8 encoder and the base64 decoder (both `utf8.encode` and `base64.decode` already return `Uint8List`), a double copy of every version-1 instruction payload, and two regular expressions that were compiled per parsed value in the fixed-point codecs.\n\nBehavior is pinned by tests rather than assumed. `base_x_property_test.dart` round-trips random byte strings across every length from 0 to 512 bytes, covers leading-zero-only input, alphabets wider than a byte, and non-power-of-two alphabets, and asserts arbitrary-precision digit strings beyond 64 bits. A degenerate one-character alphabet is now rejected with an `ArgumentError` when the codec is created; previously it produced silent zero output on encode and looped forever on decode.\n\n`bench:all` also gains coverage where the regression was invisible: the address benchmark previously measured only the all-ones System Program address, which is base58's leading-zero fast path and exercises no base conversion at all.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [b22257e](https://github.com/openbudgetfun/solana_kit/commit/b22257e1d1cb146eda742cd3da352b6a90a49f1d)"
    },
    {
      "owner_id": "solana_kit_transactions",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_transactions/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "[0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "#### Reject truncated numeric reads with `SolanaError` instead of `RangeError`\n\nA decoder reading a fixed-width field out of malformed wire data could raise a raw `RangeError` or `IndexError` out of the SDK instead of the documented `SolanaError`, so a caller catching `SolanaError` saw an escaped exception rather than a rejection it could handle. A truncated account, transaction, or RPC payload reaching the decoders was enough to trigger it; no signature or cluster access was required.\n\nThe guard that upstream `@solana/kit` applies in its number decoder factory was missing from three ports of it. `numberDecoderFactory` and `floatDecoderFactory` read through a `ByteData` view without first checking that the requested width was available, and the six `BigInt` decoders (`u64`, `i64`, `u128`, `i128`, `u256`, `i256`) indexed bytes directly with the same gap. All eight now call `assertByteArrayIsNotEmptyForCodec` and `assertByteArrayHasEnoughBytesForCodec` before reading, raising `codecsCannotDecodeEmptyByteArray` or `codecsInvalidByteLength` as upstream does. A new `bigIntDecoderFactory` carries the guard for the multi-word widths.\n\nTwo further escape sites in `solana_kit_transaction_messages` are fixed. The version 1 instruction payload slice computed `pos + numInstructionDataBytes` and sliced without confirming the buffer held that many bytes; it now asserts the length the way upstream's `fixDecoderSize` wrapper does. The transaction version decoder read `bytes[offset]` unguarded — upstream tolerates this because JavaScript yields `undefined`, which then silently takes the legacy branch and misreports a truncated buffer as an unversioned message, so this port rejects the empty buffer rather than reproducing that fallback.\n\nEncoders are unchanged and still raise `RangeError` when a destination buffer is too small. Upstream writes into a scratch buffer and then `bytes.set`s it, which throws in JavaScript too, so that behavior is deliberate parity rather than a defect.\n\nCallers that caught `RangeError` around a decode should catch `SolanaError`. Callers that already caught `SolanaError` now see malformed input rejected where it previously surfaced as a crash.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)",
              "#### Add version 1 transaction coverage with oversized payloads\n\nAdds regression tests for the version 1 transaction wire format introduced by SIMD-0296 and SIMD-0385, which raise the transaction size ceiling from 1232 to 4096 bytes.\n\n`packages/solana_kit_transactions/test/v1_transaction_test.dart` pins the exact bytes against vectors generated by upstream `@solana/kit` 8.3.0, so a regression in the v1 envelope, config mask, or config value encoding fails loudly instead of silently producing transactions the cluster rejects. The vectors live in `test/fixtures/v1_wire_vectors.json`, alongside a README documenting how to regenerate them; the largest asserts a 3216-byte transaction byte-for-byte, which no legacy transaction can represent.\n\nOn-chain coverage lands in `packages/solana_kit_integration_tests/test/integration/v1_transaction_test.dart`. It submits a v1 transaction with a 1600-byte memo — over the legacy ceiling — and asserts the node accepts it, recovers the full memo, reports the inline `transactionConfig` with the requested compute unit and loaded accounts limits, and moves lamports. The same suite asserts the node rejects a payload above 4096 bytes.\n\nA second on-chain suite, `resource_limit_estimation_test.dart`, submits a v1 transaction whose limits were produced by `estimateResourceLimitsFactory` and asserts the node executes it.\n\n`IntegrationTestEnv` gains `sendV1Instructions` and `buildV1WireTransaction` helpers, and `fetchTransaction` now requests `maxSupportedTransactionVersion: 1` so v1 responses can be read back rather than failing with RPC error `-32015`. No public library behavior changes.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## [0.10.0](https://github.com/openbudgetfun/solana_kit/releases/tag/v0.10.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n#### Reject truncated numeric reads with `SolanaError` instead of `RangeError`\n\nA decoder reading a fixed-width field out of malformed wire data could raise a raw `RangeError` or `IndexError` out of the SDK instead of the documented `SolanaError`, so a caller catching `SolanaError` saw an escaped exception rather than a rejection it could handle. A truncated account, transaction, or RPC payload reaching the decoders was enough to trigger it; no signature or cluster access was required.\n\nThe guard that upstream `@solana/kit` applies in its number decoder factory was missing from three ports of it. `numberDecoderFactory` and `floatDecoderFactory` read through a `ByteData` view without first checking that the requested width was available, and the six `BigInt` decoders (`u64`, `i64`, `u128`, `i128`, `u256`, `i256`) indexed bytes directly with the same gap. All eight now call `assertByteArrayIsNotEmptyForCodec` and `assertByteArrayHasEnoughBytesForCodec` before reading, raising `codecsCannotDecodeEmptyByteArray` or `codecsInvalidByteLength` as upstream does. A new `bigIntDecoderFactory` carries the guard for the multi-word widths.\n\nTwo further escape sites in `solana_kit_transaction_messages` are fixed. The version 1 instruction payload slice computed `pos + numInstructionDataBytes` and sliced without confirming the buffer held that many bytes; it now asserts the length the way upstream's `fixDecoderSize` wrapper does. The transaction version decoder read `bytes[offset]` unguarded — upstream tolerates this because JavaScript yields `undefined`, which then silently takes the legacy branch and misreports a truncated buffer as an unversioned message, so this port rejects the empty buffer rather than reproducing that fallback.\n\nEncoders are unchanged and still raise `RangeError` when a destination buffer is too small. Upstream writes into a scratch buffer and then `bytes.set`s it, which throws in JavaScript too, so that behavior is deliberate parity rather than a defect.\n\nCallers that caught `RangeError` around a decode should catch `SolanaError`. Callers that already caught `SolanaError` now see malformed input rejected where it previously surfaced as a crash.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [f21cd21](https://github.com/openbudgetfun/solana_kit/commit/f21cd21d26750d70f0c72e6acf8440346cf8175e)\n\n#### Add version 1 transaction coverage with oversized payloads\n\nAdds regression tests for the version 1 transaction wire format introduced by SIMD-0296 and SIMD-0385, which raise the transaction size ceiling from 1232 to 4096 bytes.\n\n`packages/solana_kit_transactions/test/v1_transaction_test.dart` pins the exact bytes against vectors generated by upstream `@solana/kit` 8.3.0, so a regression in the v1 envelope, config mask, or config value encoding fails loudly instead of silently producing transactions the cluster rejects. The vectors live in `test/fixtures/v1_wire_vectors.json`, alongside a README documenting how to regenerate them; the largest asserts a 3216-byte transaction byte-for-byte, which no legacy transaction can represent.\n\nOn-chain coverage lands in `packages/solana_kit_integration_tests/test/integration/v1_transaction_test.dart`. It submits a v1 transaction with a 1600-byte memo — over the legacy ceiling — and asserts the node accepts it, recovers the full memo, reports the inline `transactionConfig` with the requested compute unit and loaded accounts limits, and moves lamports. The same suite asserts the node rejects a payload above 4096 bytes.\n\nA second on-chain suite, `resource_limit_estimation_test.dart`, submits a v1 transaction whose limits were produced by `estimateResourceLimitsFactory` and asserts the node executes it.\n\n`IntegrationTestEnv` gains `sendV1Instructions` and `buildV1WireTransaction` helpers, and `fetchTransaction` now requests `maxSupportedTransactionVersion: 1` so v1 responses can be read back rather than failing with RPC error `-32015`. No public library behavior changes.\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_wallet_adapter",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_wallet_adapter/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "wallet [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/wallet/v0.2.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Fixes",
            "collapsed": false,
            "entries": [
              "- **Refresh analyzer excludes for the Flutter 3.47 toolchain.** The Flutter 3.47 tooling rewrites package `analysis_options.yaml` files during resolution to exclude generated `build/**` output from analysis. This applies the same exclusion to the wallet adapter and mobile wallet adapter packages (and the mobile example), keeping their analysis options stable under the updated FVM pin. No public API or behavior changes.\n  _Owner:_ Ifiok Jr. · _Introduced in:_ [e041e73](https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1)"
            ]
          }
        ]
      },
      "rendered": "## wallet [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/wallet/v0.2.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Fixes\n\n- **Refresh analyzer excludes for the Flutter 3.47 toolchain.** The Flutter 3.47 tooling rewrites package `analysis_options.yaml` files during resolution to exclude generated `build/**` output from analysis. This applies the same exclusion to the wallet adapter and mobile wallet adapter packages (and the mobile example), keeping their analysis options stable under the updated FVM pin. No public API or behavior changes.\n  _Owner:_ Ifiok Jr. · _Introduced in:_ [e041e73](https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1)"
    },
    {
      "owner_id": "solana_kit_wallet_standard",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_wallet_standard/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "wallet [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/wallet/v0.2.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          }
        ]
      },
      "rendered": "## wallet [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/wallet/v0.2.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
    },
    {
      "owner_id": "solana_kit_wallet_ui",
      "owner_kind": "package",
      "output": "default",
      "stream": "default",
      "path": "packages/solana_kit_wallet_ui/CHANGELOG.md",
      "format": "keep_a_changelog",
      "notes": {
        "title": "wallet [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/wallet/v0.2.0) (2026-09-21)",
        "summary": [],
        "sections": [
          {
            "title": "Breaking changes",
            "collapsed": false,
            "entries": [
              "#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)"
            ]
          },
          {
            "title": "Features",
            "collapsed": false,
            "entries": [
              "#### Add a hand-drawn Skribble wallet presentation\n\nThe wallet components can now render with [skribble](https://pub.dev/packages/skribble), the hand-drawn Flutter design system. `SkribbleWalletButton` mirrors the Material and Cupertino connect buttons — connect, progress, connected-account, and disconnect states — using a `WiredFilledButton` and a hand-drawn disconnect sheet, and `showSkribbleWalletPicker` opens the picker as a `WiredBottomSheet` on compact viewports or a `WiredDialog` on wide ones, with rough-inked wallet tiles and a doodle-marked empty state over the same `WalletPickerContent` behavior as the other presentations.\n\n`skribbleWalletPalette` maps a `WiredThemeData` onto the framework-neutral `WalletUiPalette` for applications that embed `WalletPickerContent` in their own hand-drawn shell, and `resolveSkribbleWalletTheme` resolves the effective theme (explicit override, then nearest `WiredTheme`, then skribble's default ink). The example app — which is also the embedded docs demo — gained a presentation selector covering Adaptive, Material, Cupertino, and Skribble so the hand-drawn wallet can be compared live.\n\nSkribble is a direct dependency, so the package's Flutter floor rises from 3.44 to 3.47 and the workspace FVM pin moves to 3.47.4 accordingly.\n\n```dart\nSkribbleWalletButton(\n  controller: walletController,\n  theme: WiredThemeData.cuddly(),\n);\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [e041e73](https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1)"
            ]
          }
        ]
      },
      "rendered": "## wallet [0.2.0](https://github.com/openbudgetfun/solana_kit/releases/tag/wallet/v0.2.0) (2026-09-21)\n\n### Breaking changes\n\n#### Raise the Dart and Flutter baseline\n\nThe workspace now builds against Dart 3.13.3 and Flutter 3.47.4, and every package declares that floor instead of the previous Dart 3.12 range. Consumers on older SDKs can no longer resolve these packages, so this release is breaking even though no Dart API changed.\n\nThe Flutter floor rises from 3.44 to 3.47 for `solana_kit_mobile_wallet_adapter`, `solana_kit_mobile_wallet_adapter_protocol`, and `solana_kit_wallet_adapter`, matching the floor `solana_kit_wallet_ui` already required. `solana_kit_lints` ships the raised floor to consumers, so it carries the same breaking bump. Every other package raises only the Dart SDK floor.\n\nRaising the language version also switches `dart format` to the tall style, so 83 files across library, test, script, and Codama-generated trees are reflowed. The renderer pipes generated output through `dart format`, so regenerating stays consistent.\n\nAlign your own SDK constraint with the workspace:\n\n```yaml\nenvironment:\n  sdk: ^3.13.0\n  # Omit for pure Dart packages; required for the Flutter packages above.\n  flutter: \">=3.47.0\"\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [5f5fe01](https://github.com/openbudgetfun/solana_kit/commit/5f5fe01f3e2220ccfee54cc26e82c3face26589d) · _Last updated in:_ [19932db](https://github.com/openbudgetfun/solana_kit/commit/19932dba1979f1190b7501947b87eb8a4d4cc8d5)\n\n### Features\n\n#### Add a hand-drawn Skribble wallet presentation\n\nThe wallet components can now render with [skribble](https://pub.dev/packages/skribble), the hand-drawn Flutter design system. `SkribbleWalletButton` mirrors the Material and Cupertino connect buttons — connect, progress, connected-account, and disconnect states — using a `WiredFilledButton` and a hand-drawn disconnect sheet, and `showSkribbleWalletPicker` opens the picker as a `WiredBottomSheet` on compact viewports or a `WiredDialog` on wide ones, with rough-inked wallet tiles and a doodle-marked empty state over the same `WalletPickerContent` behavior as the other presentations.\n\n`skribbleWalletPalette` maps a `WiredThemeData` onto the framework-neutral `WalletUiPalette` for applications that embed `WalletPickerContent` in their own hand-drawn shell, and `resolveSkribbleWalletTheme` resolves the effective theme (explicit override, then nearest `WiredTheme`, then skribble's default ink). The example app — which is also the embedded docs demo — gained a presentation selector covering Adaptive, Material, Cupertino, and Skribble so the hand-drawn wallet can be compared live.\n\nSkribble is a direct dependency, so the package's Flutter floor rises from 3.44 to 3.47 and the workspace FVM pin moves to 3.47.4 accordingly.\n\n```dart\nSkribbleWalletButton(\n  controller: walletController,\n  theme: WiredThemeData.cuddly(),\n);\n```\n\n_Owner:_ Ifiok Jr. · _Introduced in:_ [e041e73](https://github.com/openbudgetfun/solana_kit/commit/e041e731bb0291af05c06c16d7042fdbf51166a1)"
    }
  ],
  "provider": {
    "kind": "github",
    "owner": "openbudgetfun",
    "repo": "solana_kit",
    "host": null
  }
}
